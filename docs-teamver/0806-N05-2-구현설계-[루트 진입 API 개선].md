# 0806-N05-2 구현설계 — 루트 진입 API 개선

**에픽 SSOT:** [N05-1](./0806-N05-1-상위설계-[루트%20진입%20API%20호출%20베이스라인].md)  
**원칙:** 후속은 이 파일·[N05-3](./0806-N05-3-구현현황-[루트%20진입%20API%20개선].md)만 갱신.

## 현재 슬라이스 — 템플릿 상세/갤러리 UI polish loop 2

| 문제 | 수정 |
|------|------|
| Esc가 caret 메뉴와 모달을 동시 닫음 | Escape 레이어: menu → fullscreen → modal |
| slideState 전에도 ←/→ postSlide | 버튼과 동일 가드 |
| 사이드바 About/Copy 등 EN | `embedUiLabel` KO |
| dialog aria-label EN | `… 미리보기` |
| unreachable thumb hover-only retry | Retry 버튼 + stopPropagation |
| 갤러리 ↗ 22px | 28px |
| 녹색 점이 실패/로딩에도 live | `:has(fallback/skeleton)` muted |
| sidebar content reduced-motion 미적용 | animation none |

### 이전 슬라이스 — 템플릿 상세/갤러리 UI polish

Share 임시 비노출 이후 남은 크롬 이슈를 한 번에 정리.

| 문제 | 수정 |
|------|------|
| Share 숨김으로 새 탭 경로 소실 | `hideShareMenu`일 때 external-link 단독 버튼 |
| `— / —`인데 prev/next 클릭 가능 | `slideState === null`이면 양쪽 disable |
| 「템플릿 사용」과 caret 「템플릿만 사용」 중복 | caret에서 `use` 행 제거, `use-with-query`만 |
| title+nav+CTA 한 줄 밀집 | header-top(title/close) + header-toolbar(actions) |
| 긴 description이 헤더를 키움 | subtitle 제거, 사이드바 description 표시 |
| 모바일 「템플릿 정보」 긴 라벨 | compact 토글을 panel 아이콘만 |
| 갤러리 aria 영어 고정 | `embedUiLabel` KO |

### 이전 슬라이스 — Community 템플릿 deck 페이지 이동 회귀

### 증상
- Grove류 가로 `#deck` + `translateX(-N00vw)` 템플릿: host/iframe 페이지 이동 후 캔버스만 보이고 본문(`[data-anim]`) 실종
- 스테이지 우측 `‹` 핸들이 이전/다음처럼 보여 클릭 타깃이 헷갈림 · host deck-nav 아이콘이 작음

### 원인
1. `repairOverlappingSlides`가 “슬라이드가 여러 장 보인다”고 판단 → `forceRevealSlide`가 비활성 슬라이드에 `display:none !important`
2. 가로 strip이 접히면 `transformGo`의 `translateX(-N00vw)`가 빈 영역으로 이동 · native `goTo`가 `is-active`만 켜도 `!important` hide가 남아 본문 불가
3. `.ds-modal-stage-handle`의 `‹`/`›`가 페이지 셔틀로 오인

### 수정
| 파일 | 내용 |
|------|------|
| `apps/web/src/runtime/srcdoc.ts` | `transformTrack` 있으면 repair no-op · strip에서 `setSlideDisplayed` 숨김 금지 · `transformGo` → `syncTransformStripActive`(inline hide 해제 + `is-active` + dots) |
| `PreviewModal.tsx` + `composio.css` | deck-nav 아이콘 20 / hit 36px · sidebar handle → `panel-left` 아이콘 + 크기 상향 |

### 검증
- vitest: `srcdoc-transform-strip-deck` · `PreviewModal`
- staging: Grove 상세에서 4/12 이동 시 본문 표시 · 헤더 ◀▶로 이동 · 우측 핸들은 사이드바

### 이전 슬라이스 (완료)

home cover batch coalesce · Bugbot follow-up — [N05-3](./0806-N05-3-구현현황-[루트%20진입%20API%20개선].md)
