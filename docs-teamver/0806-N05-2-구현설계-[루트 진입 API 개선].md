# 0806-N05-2 구현설계 — 루트 진입 API 개선

**에픽 SSOT:** [N05-1](./0806-N05-1-상위설계-[루트%20진입%20API%20호출%20베이스라인].md)  
**원칙:** 후속은 이 파일·[N05-3](./0806-N05-3-구현현황-[루트%20진입%20API%20개선].md)만 갱신. 새 `N06+` 금지.

## 슬라이스 이력 (과거 잘못 분리된 N)

| 옛 ID | 내용 | 코드 |
|-------|------|------|
| N06 | preview-url-batch | `warmTeamverProjectPreviewPrefixes` |
| N07 | cover-html-batch | `warmTeamverHtmlCoverCache` |
| N08 | batch harden (ACL·레이스·size) | RecentProjectsStrip · project-routes |
| N09 | chip plugin boot defer | HomeView |

## 현재 슬라이스 — 카탈로그 idle defer

**목표:** slide-only home boot에서 `skills` · `design-systems` · `listTemplates`를 idle로 미룸.  
`design-templates`는 생성 경로용으로 boot 유지.

| 항목 | 동작 |
|------|------|
| gate | `shouldDeferNonCriticalEntryCatalogsOnBoot()` = embed ∧ slideOnlyMvp |
| schedule | `scheduleWhenIdle` → `requestIdleCallback` + timeout 2.5s / setTimeout fallback |
| skillsLoading | defer 시 skills 완료를 기다리지 않음 (templates만) |

**코드:** `embedDaemonFetchPolicy.ts` · `App.tsx` bootstrap  
**다음 후보:** `/files` miss-rate 추가 축소 (N05-3에 append)
