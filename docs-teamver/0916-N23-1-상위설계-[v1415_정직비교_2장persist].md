# 0916-N23-1 상위설계 · v1.4.15 정직 비교 + 2장 persist 금지

## 사실 확인 (추측 금지)

**루프546은 v1.4.15 코드를 통째로 가져온 것이 아니다.**

한 일은 이것이다.

1. **루프546** — `SLIDE_DECK_UNIQUE_SLOT_COPY_INSTRUCTION`의 penalty framing(`failed deliverable`, `majority of body slides share the same body sentence`) **한 문장만 완화**. 장 수 유지를 `SLIDE_DECK_KEEP_SLIDE_COUNT_INSTRUCTION`으로 분리. **전체 원복 아님.**
2. **그 이후 추가** (v1.4.15에 없던 것):
   - 루프547 — `padToSeedSlideCount` · `data-teamver-pad` · `findClientSlideCountRegression` severity=`warn`이면 **짧은 덱을 성공 저장(warn-save)**
   - 루프549 — incomplete-retry 경로에 pad 통합
   - 루프550 — `Return EXACTLY N` · seed 상단 힌트 · 자동 재시도 1회
   - 루프544~ 이후 kit healer (Halo / Raw-Grid / Broadside / EightBit / Block Frame 등)

0916-N13 문서 제목의 「원복」은 **penalty 문구 소프트닝**을 가리킨다. HEAD가 v1.4.15와 같다는 뜻이 아니다. 이 슬라이스도 **전체 git revert to v1.4.15를 하지 않는다.**

## 이번 실패 HTML

Block Frame / Neo-Brutalism (`nb-heading`, CSS `.slide-1`…`.slide-10` = 10장 킷)인데 **body에는 section 2장만** (slide-1 cover + slide-6 split).

- 내용: `개요` 라벨/제목 반복 · `Teamver가 다루는 문제와 제공 가치` · `문제/사용자/맥락` 일반 불릿 · `자세히 보기` CTA
- `data-teamver-pad` 없음 → **pad가 8장을 채우지 않았다**
- 직접 원인 후보: 루프547 warn-save, 또는 LOOK seed bypass / 요청 장수 미명시로 incomplete·pad가 발동하지 않은 채 2장 persist

## A. 정직한 비교 (`32effa2f9b` = v1.4.15 vs HEAD)

비교 범위: `git diff 32effa2f9b HEAD --` 아래 파일만.

| 파일 | v1.4.15 동작 | HEAD 동작 | 품질에 미친 영향 |
|---|---|---|---|
| `apps/web/src/teamver/templateCloneContentFill.ts` | `TEMPLATE_CLONE_FILL_DEFAULT_MODE='prompt'`. LOOK seed는 **look reference** (통째로 MiniMax에 보내는 경로 아님). unique-slot / topic-lock / KEEP_SLIDE_COUNT **없음**. prompt-fill은 quality / layout / density + 기존 slide-count hint만. | fill mode는 그대로 `prompt`. seed 전달 방식도 look reference 유지. 루프546부터 KEEP 라인 추가, 루프550부터 `seedShellCount` + `Return EXACTLY N` + seed 상단 힌트 + `applyQuantitativeSlideCountInstruction`. prompt-fill에는 unique-slot/topic-lock을 **넣지 않음**(546 주석). | fill mode·seed 전달은 v1.4.15와 같다. **통째 원복할 설정이 없다.** 추가된 장문·중복 장수 지시가 MiniMax를 2장에서 조기 종료시킬 수 있음. |
| `packages/contracts/src/prompts/deck-quality.ts` | export 4개: CONTENT_EXPANSION / EXAMPLE / LAYOUT_VARIETY / COPY_DENSITY. | UNIQUE_SLOT(소프트) · TOPIC_LOCK · KEEP_SLIDE_COUNT · `renderSlideCountRequirementInstruction` · `renderSlideCountSeedHeaderHint` **추가**. | penalty framing은 이미 546에서 뺌. KEEP + `Return EXACTLY N` + “If unsure, copy missing slides verbatim”가 quality 장문과 **중복 emit**되면 모델이 앞부분만 맞추고 멈출 수 있음. |
| `packages/contracts/src/template-clone-fill.ts` | LOOK merge / kit fill 없음에 가깝거나 초기 슬롯필. pad · `data-teamver-pad` · 후속 kit healer 없음. | LOOK merge + `padToSeedSlideCount`(기본 true) + kit healer 다수. merge는 leftover unparsed ≥280자면 **null → 호출부가 모델 2장 HTML을 그대로 둠**. | pad 훅은 있으나 substance gate / honorCeiling `maxSlides` / persist 호출부가 막으면 2장이 남는다. healer는 유지(원복 대상 아님). |
| `apps/web/src/components/ProjectView.tsx` | `findClientSlideCountRegression`에 severity 없음. 붕괴면 저장 거절. pad / warn-save / auto-retry 없음. | 547: severity warn이면 **2장 성공 저장**. 549: explicit 5+ incomplete만 pad. 550: 재시도 1회(요청 장수 **명시**일 때만). 이후 warn 경로는 재시도 후 `artifact-regression`(pad 없음). LOOK seed prior는 regression bypass. | **이번 HTML의 직접 원인.** 요청 미명시(또는 LOOK seed bypass)면 incomplete·retry가 안 켜지고, merge가 null이면 2장이 persist. warn-save는 짧은 덱을 성공으로 남긴다. |
| fill mode env (`TEMPLATE_CLONE_FILL_DEFAULT_MODE`) | `'prompt'` | `'prompt'` (동일) | MiniMax 10장 완성을 더 잘 뽑게 한 **기본 fill 차이 없음**. 되돌릴 것 없음. |

### v1.4.15가 10장을 더 잘 뽑게 한 설정 — 복원할 것과 복원하지 않을 것

| 항목 | v1.4.15 | 이번 결정 |
|---|---|---|
| fill mode | `prompt` | **유지** (이미 같음) |
| seed HTML 통째 전송 | 아님 (look reference) | **유지** |
| unique-slot / topic-lock in prompt-fill | 없음 | **넣지 않음** (546과 동일). JSON healer가 leftover를 덮는다. |
| KEEP_SLIDE_COUNT | 없음 | **유지하되** N이 확정되면 seed **상단** `Return EXACTLY N`만. 본문 한가운데 장문 요구·“copy verbatim” 중복 제거. |
| kit healer / canvas CSS / jobId / deck-patch / 32자 retry | 없음 | **유지** (전체 revert 금지) |

프롬프트를 짧게 하는 이유: unique-slot/topic-lock을 prompt-fill에 다시 넣지 않았고, HEAD는 KEEP + `Return EXACTLY N` + seed header + quality 장문이 **겹친다**. MiniMax가 앞 지시만 소화하고 2장에서 끊는 관측과 맞다. v1.4.15처럼 상단 한 줄만 장수를 못 박는다.

## B. persist 버그 (이번 HTML의 직접 원인)

seed/kit 10장 + MiniMax 2장일 때:

1. **warn-save로 2장만 저장 금지.**
2. 순서: (1) 자동 재시도 1회(기존 유지, 요청 미명시여도 seed 단축이면 seed N을 요청으로 간주) → (2) 재시도도 짧으면 `applyTemplateClonePromptFillLookMerge({ padToSeedSlideCount: true, forcePad: true })`로 **반드시 10장 pad** → (3) pad 실패 시에만 LOOK seed 10장 유지 + fallback 배너.
3. `findClientSlideCountRegression` severity=`warn`에서 pad를 skip하면 버그. persist **직전**에 seed/CSS 장수 대비 pad 강제.
4. `findTemplateCloneFillSlideCountIncomplete`는 explicit 5+만 본다. **seed/CSS 10 vs body 2**도 같은 recovery에 넣는다.

## C. Block Frame 내용 품질

2장(그리고 pad된 나머지)에 `개요` / `자세히 보기` / 문제-사용자-맥락 leftover가 남으면 `fillBlockFrameNeoSlots` + healer가 topic-aware로 덮는다.

- fixture: `packages/contracts/tests/fixtures/loop552-block-frame-two-slide-thin.html`
- pin: hero-label에 `개요` 반복 없음 · section 수 10 · Teamver 소개 문장 · generic `자세히 보기` wipe 또는 주제 CTA

## D. 프롬프트

KEEP_SLIDE_COUNT는 N 미확정 fallback으로 유지. N이 있으면 seed 상단 `Seed contains N. Return EXACTLY N`만. unique-slot/topic-lock은 prompt-fill에 넣지 않는다.

## 변경 이력

| 2026-09-16 18:10 | 초안 — 루프546은 전체 원복이 아님을 명시. 2장 persist 금지 + Block Frame leftover. |
