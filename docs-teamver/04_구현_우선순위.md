# Teamver ↔ Open Design — 구현 우선순위

**개발 SSOT.** 2026-06 출시 전략은 **[05 OD UI 재사용](./05_OD_UI_재사용_빠른출시.md)** + **[09 저장소·격리](./09_Design_저장소_격리_출시게이트.md)** + **[10 세션·패치](./10_세션·OD패치_보강.md)** + **[11 Usage·Drive](./11_Usage·Drive_Publish_보강.md)** 를 따른다.  
**구현 변경 누적:** **[00 구현 내역](./00_구현_내역_누적.md)** (코드 작업 시 갱신)

**관련 문서**

| 문서 | 내용 |
|------|------|
| **[09 저장소·격리 출시 게이트](./09_Design_저장소_격리_출시게이트.md)** | **Prod blocker — S3·Litestream·registry** |
| **[10 세션·OD패치 보강](./10_세션·OD패치_보강.md)** | **세션·인증·embed 브랜딩** |
| **[11 Usage·Drive Publish](./11_Usage·Drive_Publish_보강.md)** | **usage wiring · Drive Publish v1** |
| **[13 embed 슬라이드 MVP 기능 게이트](./13_embed_슬라이드_MVP_기능게이트.md)** | **embed 1차 출시 기능 범위·비노출 정책** |
| **[14 Design Drive 연동 설계](./14_Design_Drive_연동_설계.md)** | **Drive publish/import 로드맵** |
| **[05_OD_UI_재사용_빠른출시](./05_OD_UI_재사용_빠른출시.md)** | 프록시·인증·브랜딩 |
| **[07_VM_배포_인프라](./07_VM_배포_인프라.md)** | Staging/Prod VM·사양·배포 |
| [01_통합_아키텍처](./01_통합_아키텍처.md) | 아키텍처·sidecar |
| [02_design-app_daemon_연동](./02_design-app_daemon_연동.md) | wrapper BE (queue·scale) |
| [03_키_저장소_Drive_DB](./03_키_저장소_Drive_DB.md) | Drive·Publish 모델 |

**OD 참고:** [open-design-index.md](./open-design-index.md)

---

## 한 줄 결론

> **OD UI + Teamver 연동(Track A) 위에, 저장소·격리(09 Phase 0~3) + 세션·패치(10) + Usage(11 §3)를 완료한 뒤 Prod를 연다.**  
> volume-only 배포는 **공개 출시 불가**. 상세: [09](./09_Design_저장소_격리_출시게이트.md)

---

## 진행 요약

| 구간 | 상태 | 비고 |
|------|------|------|
| Track A — 연동·SSO·메뉴 | 🟡 대부분 ✅ | E2E·M2M env 남음 — [11 §3](./11_Usage·Drive_Publish_보강.md) |
| **Track A — 세션·패치 (10 §3~§4)** | 🟡 **S·P 코드 ✅** · workspace switcher ✅ · metadata branding ✅ · slide-only MVP gate ✅ · AvatarMenu/ChatPane embed gate ✅ · OD-only verify stack ✅ · staging E2E 🟡 자동화 (`run_staging_track_a_e2e.sh`) | EC2 cookie/RDS env 채워 fail-fast 회귀 |
| **Track A — Usage (11 §3)** | 🟡 hook·멱등·M2M·requestId·smoke 코드 ✅ · default OD no-op test ✅ · staging E2E 🟡 자동화 (U-6 row count psql 검증) | Phase 9 `--e2e` 로 EC2 회귀 가능 |
| **Track A — 저장소·격리 (09 Phase 0~3)** | 🟡 registry/RDS ✅ · create 전 S3 sync hard-fail ✅ · **production Phase 0 scripts ✅** · materialize·scratch sync/evict ✅ · daemon S3 init fail-fast ✅ · isolation 가드(env+container ENV+healthz) ✅ · E2E S3 object probe ✅ · smoke storage hard-fail 기본 on ✅ · staging smoke `od_storage=degraded` | **Prod blocker — EC2 S3 reachability 미확정** |
| Track A — Drive Publish (11 §6 / 09 Phase 4) | 🟡 BE+embed v1 ✅ · import UI ✅ · D-6b policy E2E ✅ · **S-8c runtime-config E2E ✅** · **D-7/D-8 publish body E2E ✅** · **publish UI 데드락 해제 ✅** · **publish 에러 phase 화 ✅** · **FE publish error UX ✅** · D-6a 실증 ☐ | G7 |
| Track B — queue·multi-replica | ☐ | 출시 후 |

---

## Track A — 출시 (연동 + 저장소)

### A. Teamver 연동 · UI

| # | 항목 | 레포 | 상태 |
|---|------|------|------|
| A1 | `deploy/teamver` — OD + design-api | `ns-open-design` | ✅ compose |
| A2 | Staging VM + nginx (`stg-design*`) | `deploy/teamver/devops/nginx` | ✅ conf |
| A2b | Production EC2 + ALB (`design*`) | `deploy/teamver/docs/DEPLOY-AWS.md` | ✅ conf |
| A3 | 로그인 게이트 + design-api SSO | `deploy/teamver` | ✅ conf + be |
| A4 | AI Apps 메뉴 | `ns-teamver-fe-v2` | ✅ |
| A5 | OD embed · `@teamver/app-sdk` BFF | `ns-open-design/apps/web` | ✅ |
| A6 | usage events + M2M | `deploy/teamver/be` + `apps/web` + `ns-teamver-be` | 🟡 hook·멱등·M2M 코드 ✅ · env·E2E ☐ |
| A6b | SDK vendor · EC2 배포 | [08](./08_Teamver_SDK_vendor와_배포.md) | ✅ vendor commit · Docker COPY · `run_docker --vendor-check-only` |
| A7 | Staging/Prod E2E (auth → session → usage → publish → S3 object → 격리) | — | 🟡 자동화 — `scripts/run_staging_track_a_e2e.sh` (Phase 9 / loop 151), EC2 cookie+RDS+S3 env 채워 fail-fast |
| A8 | Admin registry `design` | Main BE | 🟡 `seed_main_be_design_app.sh` (`--staging`/`--production` · `--verify-only`) · `run_post_deploy_track_a.sh --seed-verify` |
| A9 | Registry billing reserve/commit/refund (Phase 2) | `deploy/teamver/be` + `apps/daemon` | ✅ `services/run_lifecycle.py` + `/api/internal/billing/*` M2M + daemon `teamver-billing-bridge.ts` run-path 통합 + 구조화 `teamver_usage_5xx` CW marker + 24 unit tests (BE 6 + daemon 18) |

### B. 저장소·격리 — [09 Phase 0~3](./09_Design_저장소_격리_출시게이트.md) (**Prod 필수**)

| Phase | 한 줄 | 상태 |
|-------|------|------|
| **0** | S3 bucket · IAM · Litestream compose · scratch volume | 🟡 RDS/registry ✅ · daemon S3 init fail-fast ✅ · smoke storage hard-fail 기본 on ✅ · staging S3 probe `degraded` |
| **1** | OD `ProjectStorage` wiring + **MaterializingProjectStorage** | 🟡 lazy+run hooks ✅ · scratch-only fallback 기본 차단 ✅ · `projects.ts` 전면 ☐ (upstream 충돌 최소화 — **defer**) |
| **2** | `app.sqlite` Litestream → S3 + restore runbook | ✅ Litestream config + `restore_app_sqlite_from_s3.sh` (Litestream + snapshot 모드, `--apply` 컨테이너 적용) · prod 실증 ☐ |
| **3** | `design_projects` registry · isolation API · web list/create · daemon gate | 🟡 코드 ✅ · S3 mode create/reactivation sync hard-fail ✅ · E2E ☐ |
| **4** | Drive Publish (G7 권장) | 🟡 v1 ✅ · personal/team folder picker ✅ · 검색형 folder picker 1차 ✅ · full Drive browser/import/E2E ☐ |

**다음 추천 작업**

1. EC2 staging 에서 `OD_PROJECT_STORAGE=s3` daemon 재기동 후 `bash scripts/check_storage_isolation.sh --staging` 로 `daemon /api/health/storage ok=true` 확보.
2. 인증 cookie 포함 Track A E2E 에서 실제 project create → S3 object 존재 → Drive publish 까지 한 번에 검증 (`TEAMVER_S3_BUCKET` 포함).
3. 런타임 중 run-end `od_s3_sync_up_failed` 발생 시 사용자 UI/API 에 재시도 가능 상태를 노출할지 정책 결정.

**전체 체크리스트·env:** [09 §4·§7~§14](./09_Design_저장소_격리_출시게이트.md) · [10 §5~§6](./10_세션·OD패치_보강.md) · [11 §7~§8](./11_Usage·Drive_Publish_보강.md)

### C. 세션·패치 — [10 §3~§4](./10_세션·OD패치_보강.md) (**출시 전 P0/P1**)

| Phase | 한 줄 | 상태 |
|-------|------|------|
| **S** | session 401·refresh·design-api nginx·FE auth recovery·Main BE identity headers·nginx auth_request_set·runtime-config | 🟡 코드·설정 ✅ · E2E ☐ |
| **P** | TeamverBrandingProvider·embed UI·execution lock·브랜딩 env | ✅ — [10 §4](./10_세션·OD패치_보강.md) · smoke ☐ |

### D. Usage·Drive — [11](./11_Usage·Drive_Publish_보강.md)

| Phase | 한 줄 | 상태 |
|-------|------|------|
| **U** | FE usage hook·멱등·Main BE design M2M·requestId | 🟡 코드 ✅ · env·E2E ☐ |
| **D** | Drive Publish v1 (HTML+ZIP) — G7 | 🟡 BE+embed ✅ · personal/team folder picker ✅ · 검색형 folder picker 1차 ✅ · full Drive browser/import/E2E ☐ |

### E. 출시 후 (Track A에서 제외)

| # | 항목 |
|---|------|
| — | Teamver 전용 DESIGN.md/SKILL.md |
| — | `@open-design/teamver-integration` 런타임 (headless) |

---

## Track B — wrapper · scale (출시 후)

[02](./02_design-app_daemon_연동.md) — job/run API, queue, circuit breaker, multi-replica.

| # | 항목 | 레포 | 상태 |
|---|------|------|------|
| B1 | job/run API | `deploy/teamver/be` | ☐ |
| B2 | `@open-design/teamver-integration` | `ns-open-design` | ✅ 패키지 |
| B3 | Drive · usage (wrapper) | `ns-teamver-platform` | 🟡 SDK ✅ |
| B4 | circuit breaker · 슬롯 | wrapper BE | ☐ |
| B5 | OD `DaemonDb` Postgres | `apps/daemon` | ☐ upstream Phase 5 |

---

## Track C — 차별화 (선택)

| # | 항목 | 상태 |
|---|------|------|
| C1 | Teamver 전용 `DESIGN.md` / `SKILL.md` | ☐ |
| C2 | Teamver 네이티브 FE | ☐ |

---

## 레포별 책임 (출시 기준)

| 레포 | Track A (출시) | Track B (나중) |
|------|----------------|----------------|
| `ns-open-design` | deploy, web patch, **OD storage wiring**, docs-teamver | teamver-integration, queue |
| `ns-teamver-devops` | **S3 · IAM** (09 P0) | — |
| `ns-teamver-fe-v2` | 앱 메뉴 | Drive UX |
| `ns-teamver-platform` | app-sdk | Drive API |
| `ns-teamver-be` | design AppKey, session-check | registry |

---

## 결정 기록

| 일자 | 결정 |
|------|------|
| 2026-06-15 | **volume-only prod 불가** — [09](./09_Design_저장소_격리_출시게이트.md) Phase 0~3 = 출시 게이트 |
| 2026-06-15 | Track A 우선 — OD UI 재사용 |
| 2026-06-15 | Staging/Production VM 분리 — [07](./07_VM_배포_인프라.md) |
| 2026-06-15 | Track B = queue·scale·Postgres DaemonDb (출시 후) |

---

## 변경 이력

| 일자 | 내용 |
|------|------|
| 2026-06-19 | **D-8 publish 207 partial driveAssetId E2E (loop 181)** — 207 partial 응답에서 ready output 의 `driveAssetId` non-empty 검증. `TEAMVER_PUBLISH_ARTIFACT_FILE` / `TEAMVER_PUBLISH_FORMATS` env 로 staging publish body 지정. fixture 207 partial pass/fail 시나리오 2개 추가 |
| 2026-06-19 | **FE publish phase error UX (loop 180)** — `formatPublishErrorCodeForUser()` 로 loop-177 phase code → 사용자 toast 힌트 매핑. `drive_upload_failed_403` → session expired, `drive_presigned_put_failed_*` → storage upload failed 등 |
| 2026-06-19 | **Production Phase 0 S3/RDS scripts (loop 179)** — `print/apply/run_production_phase0_activate` + 3 fixture scripts. `DEPLOY-AWS.md` / `07` production activation 절차 |
| 2026-06-19 | **D-7 publish body verification (loop 178)** — `run_staging_track_a_e2e.sh` D-5 가 publish 응답 body 까지 capture 해서 201/207/502 별로 `outputs[].driveAssetId` / `errorCode` 모양을 검증. 신규 D-7 phase 가 Drive 업로드 누락 회귀를 자동 검출. `test_run_staging_track_a_e2e.sh` 에 *empty driveAssetId* 시나리오 추가 |
| 2026-06-19 | **publish 에러 phase 화 + 구조화 로그 (loop 177)** — `publish_service.py` 의 Drive 업로드를 `upload_request → presigned_put → confirm` 3-phase 분리. 각 phase 가 자체 error_code (`drive_upload_failed_<status>` / `drive_presigned_put_failed_<status>` / `drive_confirm_failed_<base>`) 를 노출하고 phase + status 를 한 줄 `logger.warning` 으로 흘림. SDK private `_put_presigned_bytes` 는 `_drive_presigned_put` wrapper 로 격리. pytest +3 회귀 |
| 2026-06-19 | **Drive Publish 메뉴 데드락 해제 + 에러 가시화 (loop 176)** — `TeamverPublishDriveMenuItem` 가 `targets.length === 0` 일 때 select+Browse 둘 다 disabled 되던 데드락 제거. 항상 `personal-default` 폴백 옵션 유지, workspace bridge 부팅 중에는 "연결 중" 힌트, `listTeamverDrivePublishTargets` throw 시에는 "Browse 로 다시 시도하세요" 힌트(`aria-live=polite`). Browse 클릭이 `targetsError` 상태에서 자동 refetch 트리거. vitest +3 회귀 |
| 2026-06-19 | **embed share/publish 정책 — 외부 share 전부 hide (loop 171)** — `hideExternalShareSurfaces` 게이트 추가. `FileViewer` chrome share-menu (Vercel/Cloudflare publish, share-link 복사·열기, Project social share `SocialShareGrid`) embed에서 미렌더. `PreviewModal` share popover의 social platforms (X/Reddit/FB/LinkedIn/IG/小红书) + Copy link/Copy share text 항목 hide. `ProjectView` `onShareToOpenDesign` (community contribute → `SHARE_TO_COMMUNITY_PROMPT`) callback 차단 → AssistantMessage submission 미노출. **Drive Publish + 로컬 export (PDF/PPTX/Image/HTML/Markdown/ZIP/Save as template) 는 그대로 유지**. 추가 share 기능 개발은 MVP 제외, 향후 필요 시 Drive 권한 모델로 통합. 정적 핀 테스트 + `13` §2.7 share/publish 정책 표 추가 |
| 2026-06-19 | **Drive publish folder choose action + E2E negative guard (loop 169)** — publish picker에 `Use this folder` footer 액션 추가(드릴다운/선택 완료 분리). D-6a fixture가 `TEAMVER_DRIVE_IMPORT_FILENAME` body 전달과 `201 + imported: []` 실패를 검증. 남음: staging 실계정 D-6a 실행 |
| 2026-06-19 | **Drive publish browser + D-6a filename env (loop 168)** — `driveApi.ts` 공용 Main Drive client 추출, publish picker에 personal/team Drive scope tab + breadcrumb + folder drill-down 추가. D-6a E2E `TEAMVER_DRIVE_IMPORT_FILENAME` 지원 및 `imported[]` non-empty 검증. 남음: staging 실계정으로 D-6a 실행 |
| 2026-06-19 | **Drive publish server-side folder search (loop 167)** — publish picker가 2글자 이상 검색 시 Main Drive search/list API를 호출해 200개 flatten 목록 밖의 개인/팀 Drive folder도 destination으로 선택. `teamver-embed-config-lock` fixture 타입 보정으로 `npm run typecheck` 복구. 남음: D-6a 실 Drive asset 실증 |
| 2026-06-19 | **slide/API chat E2E gate + usage regression wiring (loop 166)** — S-8c runtime-config probe, loop 165 tests in Track A runner |
| 2026-06-19 | **embed API-mode usage billing + execution pin (loop 165)** — proxy usage SSE, pinned runtime-config, usage attribution fix |
| 2026-06-19 | **Drive import analytics contracts (loop 164)** — `DriveImportModalSurfaceViewProps` / `DriveImportModalClickProps` in contracts + typed track helpers. 남음: D-6a 실 Drive asset 실증 |
| 2026-06-19 | **D-6b import-drive policy E2E + env helper (loop 163)** — policy reject 502 probe, `print_staging_track_a_e2e_env.sh`. 남음: D-6a 실 Drive asset 실증 |
| 2026-06-19 | **BE Drive import allowlist 정합 (loop 162)** — `drive_import_policy.py` + service per-asset `failed[]`, FE `embedFileAttachPolicy` 동기화. 남음: staging import E2E 실증 |
| 2026-06-19 | **embed attach policy + Drive import analytics (loop 161)** — `embedFileAttachPolicy`, composer upload/import 필터, modal analytics |
| 2026-06-19 | **Drive import asset grid + thumbnails (loop 160)** — grid 카드 UI, object-url batch 썸네일, file-type 아이콘. 남음: staging import E2E 실증 |
| 2026-06-19 | **Track A E2E D-6 import-drive (loop 159)** — staging E2E에 `import-drive` curl probe + restore fixture design_compose 복사 수정 |
| 2026-06-19 | **Drive import browser 고도화 (loop 158)** — recent 섹션, server search(debounce), breadcrumb navigation stack. 남음: asset grid/thumbnail · staging E2E 실증 |
| 2026-06-19 | **Drive import composer modal (Phase 2-1)** — `TeamverDriveImportModal` + `ComposerPlusMenu` Drive row + `ChatComposer` import→staged attachments. 남음: full Drive browser/search grid (loop 157) |
| 2026-06-19 | **Design deploy.sh (BE/Slide 패턴)** — `.env` symlink 제거, `design_compose.sh`, staging `TEAMVER_OD_API_KEY` gate, smoke `configured=true` (loop 156) |
| 2026-06-19 | **Drive import design-api + FE client foundation** — `POST /api/v1/projects/{projectRef}/import-drive` 추가. SDK `drive.download_bytes` → daemon `/upload` multipart → scratch/S3 materialization 경로로 개인/팀 Drive asset을 프로젝트 파일로 ingest. batch 12개 제한, path traversal/실행 파일 확장자 차단, 201/207/502 결과 계약과 FE `importDriveAssets.ts` 클라이언트/테스트 추가. 남음: full Drive browser (loop 155 foundation) |
| 2026-06-19 | **run_docker sidecar readiness wait** — compose up 직후 design-api `/api/healthz` 와 daemon `/api/health` loopback 대기(최대 120초), runtime-config seed 전 readiness 확인. `.env` source 없이 `BE_PORT`/`OD_PORT` 안전 파싱, `test_run_docker_wait_ready.sh` Track A runner 연결 (loop 154) |
| 2026-06-19 | **Drive publish 검색형 folder picker 1차** — `TeamverDrivePickerModal` 추가, publish menu `Browse` 버튼으로 개인/팀 Drive folder target 검색·선택, 모달용 target limit 200, 선택한 `folderId/sharedDriveId` publish payload 검증. Track A runner에 `teamver-publish-drive-menu-item.test.tsx` 포함 (loop 153) |
| 2026-06-19 | **embed slide-only MVP 게이트 마무리** — `hideCommunityGallery`/`hidePluginRegistry` 추가, Home community gallery·plugin registry 진입점 비노출, ChatComposer/NextStepActions toolbox action 필터 연결(`image-gen`/`video-gen`/`motion`/`motion-polish` 제거), Drive 연동 설계 문서 14 추가, Track A runner에 embed slide-only/branding 테스트 포함 (loop 152) |
| 2026-06-19 | **Track A staging E2E 자동화 (loop-142-launch-gates §4)** — `run_staging_track_a_e2e.sh` 를 curl + RDS psql 자동 회귀로 전면 재작성: S-8a `/api/auth/session`, S-8b `/api/v1/projects`, U-6 `/api/internal/usage/events` + `ai_model_token_usages` row count 1 멱등, D-5 `/publish` + `design_outputs.od_project_id` row, 다중 사용자 `/access` 403 격리. `run_post_deploy_track_a.sh --e2e` Phase 9 통합. 신규 fixture `test_run_staging_track_a_e2e.sh` (mock curl 4 시나리오) Track A runner 연결. 출시 게이트 P0 4종 closure (loop 151) |
| 2026-06-19 | **storage smoke/prod env 출시 게이트 hardening** — staging/production `smoke_design.sh` 가 `SMOKE_REQUIRE_OD_STORAGE=1` 기본 on, post-deploy smoke도 storage hard-fail. `validate_deploy_env.sh --production` 이 managed/daemon LLM key 누락·정적 AWS key·staging token 재사용을 hard fail. `test_smoke_design_storage_default.sh` Track A runner 연결 (loop 150) |
| 2026-06-19 | **OD core verify seed compose-file 정합** — `run_od_core_verify.sh seed` 가 격리 compose file을 seed 스크립트에 전달하도록 수정. `seed_od_runtime_config.sh` / `seed_od_byok_app_config.sh` 에 `--compose-file` 지원 추가 (loop 149) |
| 2026-06-19 | **Drive publish target picker 회귀 Track A runner 포함** — `teamver-drive-publish-targets.test.ts` 를 Track A web vitest 목록에 추가해 개인/팀 드라이브 target 정규화와 팀 드라이브 하위 폴더 flatten 회귀를 기본 gate로 검출 (loop 148) |
| 2026-06-19 | **OD core-only 검증 스택 + BYOK seed runbook** — `:7457` 격리 daemon compose, `run_od_core_verify.sh`, `verify_od_core.sh`, BYOK app-config seed, `docs-teamver/13` 추가. `seed_od_runtime_config.sh --service` 버그 수정, Track A fixture 연결 (loop 147) |
| 2026-06-18 | **Drive Publish 팀 드라이브 하위 폴더 선택** — 팀 드라이브 `folder-tree` 하위 폴더를 flatten해 `sharedDriveId + folderId` target으로 노출. web vitest 15 + typecheck 통과. 남음: 검색형 브라우저 UX + staging E2E (loop 146) |
| 2026-06-18 | **Drive Publish 대상 선택 UX 1차** — embed publish menu에서 Main Drive API로 개인 드라이브 루트/폴더 + 팀 드라이브 루트를 조회해 `folderId/sharedDriveId`로 저장. `VITE_TEAMVER_DRIVE_PUBLISH_SHARED_DRIVE_ID` 운영 기본값 추가. 남음: 전체 폴더 브라우저/검색 + staging E2E (loop 142) |
| 2026-06-18 | **design-api DB 5xx 운영 가시성 + apply idempotency** — design-api 에 SQLAlchemy `DBAPIError`/`SQLAlchemyError` 전역 핸들러 추가. 응답은 `503 db_unavailable`, 동시에 구조화 `teamver_design_api_db_5xx` JSON 마커 emit (`error_kind: ssl_verify/timeout/connect/auth/pg_hba/operational`, chained `__cause__` walk). `print_cloudwatch_alarm_commands.sh` 12)/13) 필터·알람 추가 → loop 138 RDS SSL 같은 incident 5분 내 알람. `apply_staging_s3_env.sh` idempotency 회귀 fixture(연속 실행 시 키 중복/섹션 중복 금지), `print_track_a_status.sh` 가 staging/prod 인데 `OD_PROJECT_STORAGE != s3` 면 ✗ 마킹. design-api pytest 93 통과 (loop 141) |
| 2026-06-18 | **storage isolation 가드 + check_storage_isolation.sh** — `validate_deploy_env.sh` 가 `.env.staging`/`.env.production` 에서 `OD_PROJECT_STORAGE=s3` 누락·MinIO endpoint 사용을 **hard fail**. `.env.*.example` 의 `OD_PROJECT_STORAGE=s3`/`OD_S3_BUCKET`/`OD_S3_REGION` 을 주석 해제(필수). 신규 `scripts/check_storage_isolation.sh` 가 (1) .env 파일, (2) daemon/design-api 컨테이너 ENV `docker exec` 확인, (3) design-api `/api/healthz/deps` `config.project_storage=s3` + `checks.od_storage=ok` + `checks.db=ok`, (4) daemon `/api/health/storage` `mode=s3` + `ok=true`, (5) `TEAMVER_API_BASE_URL`(Drive publish) 를 검사. `run_post_deploy_track_a.sh` Phase 8 으로 자동 호출 → 실패 시 배포 중단. 신규 fixture `test_check_storage_isolation.sh` + `test_validate_deploy_env_storage.sh` (loop 140) |
| 2026-06-18 | **design-api RDS asyncpg SSL fix + AvatarMenu/ChatPane embed gate** — `app/db/connection.py` `_connect_args()` 가 `POSTGRES_SSLMODE=require`/`prefer`/`""` 시 `check_hostname=False` + `verify_mode=CERT_NONE` SSL context 명시 → AWS RDS self-signed CA chain 에서 `SSLCertVerificationError` 로 502 떨어지던 `/api/projects/:id/events` 회복 (libpq `require` 의미와 정합). `AvatarMenu` "Use Local CLI / Use API" 토글, `ChatPane` BYOK recovery / "Switch to Local CLI" CTA 를 `hideStudioExecutionControls` (TeamverBranding) 로 embed mode 에서 hide. 신규 회귀 테스트: `tests/test_db_connection_ssl.py` (6 case), `AvatarMenu.test.tsx`/`ChatPane.streaming.test.tsx` (loop 138–139) |
| 2026-06-18 | **Drive Publish 개인/팀 드라이브 대상 지정 API** — publish request/response에 `folderId` + `sharedDriveId`, `design_outputs.drive_shared_drive_id`, Main BE Drive presigned 3-step 업로드 전환. 남음: workspace Drive picker + staging E2E (loop 139) |
| 2026-06-18 | **staging smoke S3 판정 강화** — 현장 smoke에서 registry/RDS는 OK, `checks.od_storage=degraded`로 S3 저장은 아직 OK 아님. smoke가 deps `config.project_storage=s3` 기준으로 storage degraded를 fail 처리하고 public `/api/health/storage` 302는 nginx auth gate로 분류 (loop 137) |
| 2026-06-18 | **run_docker SDK vendor preflight 강화** — compose build 전 manifest/tgz/Python wheel 전체 확인, 누락 시 sync 재시도 후 명확히 fail, `--vendor-check-only` 운영 preflight 추가 (loop 136) |
| 2026-06-18 | **SDK vendor EC2 build readiness 완료** — `vendor/teamver` tarball·wheel·manifest 추적 확인, `deploy/Dockerfile` pnpm install 전 COPY + design-api wheel install 정합, `pnpm check:teamver-vendor`/`node scripts/check-teamver-vendor.mjs` gate 기준 명시 (loop 135) |
| 2026-06-18 | **staging 공유 RDS DB 생성 전제 명시** — `.env.staging.example` 을 `teamver-staging-postgres`/`teamver_be_admin` 기준으로 정합, `run_staging_phase0_activate.sh` 가 `CREATE DATABASE teamver_design_staging OWNER teamver_be_admin;` prerequisite 출력 (loop 134) |
| 2026-06-18 | **terraform RDS+S3 → .env.staging merge** — `print_staging_s3_env --from-terraform` RDS keys, terraform post_deploy checklist, DEPLOY-AWS runbook (loop 137) |
| 2026-06-18 | **Phase 0 staging activation runner + Main BE env snippet** — `run_staging_phase0_activate.sh`, `print_main_be_design_env.sh`, Main BE `.env.staging.example` A6 (loop 132) |
| 2026-06-18 | **deploy layer S3 purge env + Main BE wiring check** — docker-compose storage/billing env passthrough, `check_main_be_design_wiring.sh` (A6 read-only), sidecar scratch/evict probe. OD core `projects.ts` 전면 리팩터는 upstream sync 위해 defer. (loop 131) |
| 2026-06-18 | **od_s3_remote_purged CW alarm + onProjectRemoved tests** — purge partial-failure metric filter/alarm, lazy hooks purge-on/off unit tests, Track A runner 승격 (loop 130) |
| 2026-06-18 | **registry delete S3 tenant purge** — `deleteAllUnderObjectPrefix` + `onProjectRemoved` remote purge (`od_s3_remote_purged`), `OD_S3_PURGE_ON_DELETE` knob, legacy DELETE route 제거. design-api `scratch/evict` 경로로 자동 purge. (loop 126–129) |
| 2026-06-18 | **embed AMR/vela 라이프사이클 헬퍼 no-op + usage-bridge CW 마커 wire-compat** — `startVelaLogin/cancelVelaLogin/velaLogout` 도 embed 시 fetch 없이 graceful 결과 반환(`amr_disabled_in_embed` / `{ok:true, canceled:false}` / `{ok:true}`). `teamver-usage-bridge` non-ok 응답·throw 가 free-form `console.warn` 대신 구조화 `{metric:"teamver_usage_5xx", stage:"usage.events", ...}` 마커 emit — `print_cloudwatch_alarm_commands.sh` 의 `'"teamver_usage_5xx"'` literal filter 가 billing + usage 양쪽 잡도록 통일. 누적: design-api 79 / ops 9 / daemon 56 (7 files, +3 usage marker) / web 66 (14 files, +6 amr-embed-skip) (loop 125) |
| 2026-06-18 | **Teamver embed AMR/Vela FE polling no-op** — embed mode에서 `fetchAmrModels` / `fetchVelaLoginStatus` 가 daemon AMR/Vela endpoint 호출 없이 `null` 반환, staging vela 미설치 503 polling noise 제거. Track A runner web 70개 통과 (loop 124) |
| 2026-06-18 | **Track A runner periodic scratch sampler 포함** — `run_track_a_unit_tests.sh` daemon vitest에 `project-materialization-runtime.periodic.test.ts` 추가, drain marker idempotency를 기본 gate로 승격 (loop 123) |
| 2026-06-18 | **SIGTERM drain + project-access CW alarm + od_storage smoke gate** — daemon `shutdownDaemonRuns` 가 dispose()로 drain 마커 emit (idempotent), CW `teamver_project_access_5xx` filter/alarm, smoke `OD_PROJECT_STORAGE=s3` 시 od_storage fail, status probe `/api/health/storage`. 검증: Track A runner design-api 79 / ops 9 / daemon teamver 48 / web 56 + periodic scratch sampler 5 (loop 120–122) |
| 2026-06-17 | **502/500 staging 진단성 보강** — daemon `teamver-project-access` 미들웨어 catch 경로에 `teamver_project_access_5xx` 구조화 warn 마커(`reason: timeout|network|http_5xx`, `projectId/workspaceId/httpStatus|timeoutMs|elapsedMs`) emit. `/api/amr/models` 가 vela 바이너리 미설치 시 graceful **503** + `{available:false, reason, models:[], presets:[]}` (새 helper `classifyAmrModelProbeError`). `.env.staging`/`.example` `TEAMVER_PROJECT_ACCESS_TIMEOUT_MS` 2500 → **5000**. `test_health_deps` 회귀 수정. 누적: design-api 79 / ops 9 / daemon 48 (6 files) / web 56 (loop 119) |
| 2026-06-17 | **scratch drain + healthz S3 broker + restore self-probe** — daemon `dispose()` 가 stage="drain" 마커 best-effort emit, `ProjectStorage.probe()` (S3 list-type=2&max-keys=1 + Local mkdir+access) 추가, daemon `/api/health/storage` 신규 엔드포인트(200 ok / 503 ok:false / 504 timeout / 503 storage_not_initialized) + design-api `_check_od_storage` broker + `checks.od_storage` 표면, smoke_design.sh 가 od_storage 분류 + daemon-direct 프로브 + restore 스크립트 `--dry-run` self-probe. 누적: design-api 79 / ops 9 / daemon 45 / web 56 (loop 116–118) |
| 2026-06-17 | **backup_sqlite_to_s3 dry-run + project-storage-meta unit test** — `backup_sqlite_to_s3.sh` `--dry-run`을 fixture-friendly로 정렬(`DRYRUN: docker compose stop/cp`, `DRYRUN: aws s3 cp` echo, `mode=fallback env=… bucket=… dest=…` 헤더, `usage()`에 `--prefix`/09 P2-3 reference 보강) + `test_backup_sqlite_to_s3.sh` (NEW, consistency guard·`OD_S3_BUCKET` fallback·`--prefix` override·LATEST.json 등) + `tests/teamver-project-storage-meta.test.ts` (NEW, 13 case — `fetchTeamverProjectS3Prefix` 7 + `resolveTeamverTenantRemoteStorage` 6). 누적: design-api 74 / ops 9 / daemon 40 / web 56 (loop 115) |
| 2026-06-17 | **daemon billing bridge 구조화 5xx 마커 + Track A daemon vitest** — `teamver-billing-bridge.ts` 실패 경로를 구조화 JSON 마커(`{metric, stage, runId, workspaceId, amount, ...}`)로 emit, `TEAMVER_BILLING_TIMEOUT_MS` env 도입, `run_track_a_unit_tests.sh`에 daemon vitest 단계 통합, bridge 18 case (loop 111) |
| 2026-06-17 | **scratch disk metric + lifecycle + restore + reserve knob** — daemon `od_scratch_disk_usage` JSON 마커 + CW filter/alarm, `scripts/s3_lifecycle_policy.sh` (sqlite-backups expire + scratch evict + multipart cleanup, `--apply`/`--diff`), `scripts/restore_app_sqlite_from_s3.sh` (Litestream + snapshot 모드, `--apply` 컨테이너 적용), daemon `TEAMVER_BILLING_RESERVE_AMOUNT` env knob + A9 status ✅ (loop 107–111) |
| 2026-06-17 | **daemon billing bridge + nginx deny** — `teamver-billing-bridge.ts` reserve/commit/refund M2M, server.ts run lifecycle hook, stg-design-api `/api/internal/` deny 보강, smoke commit/refund probe, ops billing bridge status·warn (loop 102–105) |
| 2026-06-17 | **storage hardening** — lazy materialization (media/finalize/deploy/plugins/design-system) 커버, run-end `od_s3_sync_up_failed` JSON 마커, `validate_deploy_env` drive/scratch warn, `test_run_s3_integration_test` fixture (loop 97–100) |
| 2026-06-17 | **internal billing endpoints** — `/api/internal/billing/{reserve,commit,refund}` (M2M) + smoke·sidecar·status probe·registry creds preflight (loop 92–95) |
| 2026-06-17 | **ops billing + CW alarm** — `print_cloudwatch_alarm_commands.sh` usage 5xx alarm, `run_lifecycle` orchestrator, `run_post_deploy_track_a --seed-verify` (loop 87–90) |
| 2026-06-17 | **ops A8·sidecar gates** — seed `--verify-only`, check_sidecar linkedDirs/import probes |
| 2026-06-17 | **embed P2 로컬 UX** — DS code folder, linkedDirs policy, daemon hint — [12](./12_embed_로컬UX_제거_체크리스트.md) |
| 2026-06-17 | **embed 로컬 UX 제거** — working directory / folder import 숨김 — [12](./12_embed_로컬UX_제거_체크리스트.md) |
| 2026-06-17 | **smoke 302 auth gate** — public design-api smoke가 nginx login redirect(302)를 정상 auth gate로 인식 |
| 2026-06-17 | **metadata branding** — embed build OG/title/favicon/loading shell Teamver화 |
| 2026-06-17 | **workspace switcher** — Main session workspaces 기반 embed WS 전환, Track A unit runner 보강 |
| 2026-06-17 | **기본 OD 회귀 방지** — Teamver gate 미설정 project routes, usage BFF client 없음 no-op 테스트 추가 |
| 2026-06-15 | **코드 반영** — 10 §3·11 §3 구현 완료 항목 진행 표 갱신 |
| 2026-06-15 | **10·11 연동 보강** — 세션·패치·Usage·Drive Publish SSOT |
| 2026-06-15 | **09 §7~17** — FUSE vs Hybrid, MaterializingStorage, 격리·IAM·장애·마이그레이션 |
| 2026-06-15 | **09 저장소·격리** — Track A prod blocker, Phase 0~4 진행 표 |
| 2026-06-15 | Track A/B/C 재구성 — 빠른 출시(OD UI) 1순위 |
| 2026-06-15 | `docs/` flat 구조 + `open-design-index.md` |
