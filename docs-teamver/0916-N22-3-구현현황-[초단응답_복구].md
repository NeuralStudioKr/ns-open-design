# 0916-N22-3 구현현황 · 초단 HTML 응답 persist 복구 (루프552)

상위설계: `docs-teamver/0916-N22-1-상위설계-[초단응답_복구].md`  
사용자 리포트: project `26b65b55-01c1-439f-9de5-e0f41e172a97` · conversation `abcf0b77-52e6-46bc-ba54-962b2c10cddb` · `content too short to be HTML (got 32 chars, need ≥64)` · `terminalPersistResultKind=rejected` · `incomplete_output`.

## 요약

32자/`<64` HTML은 `deck.html`에 쓰지 않는다. create/full fill + LOOK seed/prior면 루프550과 같은 `autoRetryForShortResponse` 1회 재시도. 재시도도 짧으면 seed 유지 + `clone_look_seed_fallback`. seed/prior 없을 때만 기존 거절 카피 + Retry.

## 구현 체크

- [x] 거절 지점: `validateHtmlArtifact` MIN 64 → persist `rejected` → incomplete_output
- [x] `decideTooShortHtmlPersistRecovery` — retry / keep-seed / reject / scoped
- [x] persist: 재시도 → `needs-short-response-retry` + `retryKind: 'too-short-html'`
- [x] persist: keep-seed → `skipped-incomplete` (32자 미저장) → 기존 LOOK seed Retry dock
- [x] persist: seed/prior 없음 → 기존 reject 카피. `surfaceChatVisibleError` 유지
- [x] scoped image/comment → 재시도 없음. 기존 helper/AC 경로
- [x] 재시도 프롬프트: `previous output was not HTML (got N chars, need ≥64)`
- [x] `autoRetryForShortResponse` 이미 true면 재재시도 없음
- [x] ≥64 짧은-but-valid HTML 저장 경로 유지
- [x] 루프544–551 pad / severity / healer / KEEP_SLIDE_COUNT / slide-count retry 미되돌림
- [x] Product Launch healer / kit 파일 미수정

## 32자 추정

staging 본문은 이 슬라이스에서 직접 못 읽음. `got 32 chars`는 JS `trim().length`. 후보는 모델 self-talk (`I'll generate the slides now` 계열, 29–32자), 빈 `<html>` 셸(26–39자), 짧은 prose/`OK`. 재시도 프롬프트에 preview를 넣어 다음 관측에서 구분한다.

## 이전 vs 지금

| 시나리오 | 이전 | 지금 |
|---|---|---|
| 32자 × LOOK seed · 재시도 성공 | reject · incomplete_output | **재시도 1회 → 저장 · notice 없음** |
| 32자 × LOOK seed · 재시도도 32자 | reject · seed 안 염 | **seed 유지 + clone_look_seed_fallback**. 32자 미저장 |
| 32자 × seed 없음 × prior 없음 | reject · incomplete_output | **상동 reject** + Retry |
| 32자 × scoped image/comment | reject / AC helper | **재시도 없음**. 기존 scoped/AC. 32자 미저장 |
| ≥64 짧은-but-valid HTML | 저장 | 저장 |
| `autoRetryForShortResponse` 이미 true | — | **재재시도 없음** → keep-seed 또는 reject |

## 바꾼 파일

- `apps/web/src/artifacts/validate.ts` — too-short / not-HTML reason helpers
- `apps/web/src/teamver/shortResponseAutoRetry.ts` — 루프552 결정·프롬프트
- `apps/web/src/teamver/tooShortHtmlPersist.ts` — persist 복구 결과 (retry / keep-seed / scoped)
- `apps/web/src/components/ProjectView.tsx` — persist 분기 + 재시도 프롬프트 + seed recovery에 rejected too-short
- `apps/web/tests/teamver/short-response-auto-retry.test.ts`
- `apps/web/tests/artifacts/validate.test.ts`
- `apps/web/tests/project-view-message-load.test.ts`

## 검증

- web `short-response-auto-retry` · `validate` 32자 pin · persist 소스 pin

## 사용자 재현

같은 project `26b65b55-01c1-439f-9de5-e0f41e172a97`에서 생성을 다시 보낸다. LOOK seed가 있으면 재시도 후 저장되거나 seed+Retry. 32자 쓰레기는 `deck.html`에 없어야 한다.

## 변경 이력

| 2026-09-16 18:05 | 초단 HTML을 거절 대신 seed 유지+1회 재시도로 복구. |
