# 0908-N03-3 구현현황 — 킷 대비 순응 · 찌꺼기 (루프478)

상위: [0908-N03-1](./0908-N03-1-상위설계-[킷_대비순응_이중패딩_찌꺼기].md) · 설계: [0908-N03-2](./0908-N03-2-구현설계-[킷_대비순응_이중패딩_찌꺼기].md)

## 완료

### 1. official look 대비 순응 ☑

신규 `packages/contracts/src/html/heal-official-look-contrast.ts` — `conformInlinePaletteToOfficialLook`.

- look CSS의 paper/ink 토큰을 해석해 게이트(둘 다 없거나 명도비 < 3이면 no-op)
- 슬라이드 섹션마다 태그를 순회하며 유효 배경 명도를 스택 추적 — hex · `rgb()`/`rgba()`(알파 합성) · `var(--token)` · gradient(정지점 평균)
- 인라인 `color`가 유효 배경과 WCAG 명도비 3 미만이면 ink/paper hex로 스냅
- 밝은 배경 위 거의 투명한 흰 카드는 `rgba(ink,0.055)` 배경 + `rgba(ink,0.2)` 보더로 승격
- look CSS가 배경을 칠하는 class를 가진 서브트리와 `data-od-official-motif-html`은 skip
- `healAiGeneratedDeckMarkup` 종단에 배선 → persist · srcdoc 프리뷰 · preview look heal · PDF export 공통

`tests/heal-official-look-contrast.test.ts` 9케이스 통과 — 크림 위 `#93c5fd`/`#e0e7ff` 스냅, 유리 카드 틴트, 다크 카드 위 accent 보존, 킷 class 서브트리 no-op, 모티프 no-op, look CSS 없으면 no-op, 멱등성.

### 2. 찌꺼기 스크럽 ☑

- `stripStrayInlineAcronyms` 목록에 `R&D`(`R&amp;D`)·`SaaS`·`SSO`·`UX`·`UI`·`ROI`·`B2B`·`B2C`·`PoC`·`CRM`·`ERP`·`SDK`·`SLA`·`QA`·`IoT` 추가
- `stripStrayBracketTextNodes` 신규 — 블록 태그 사이/닫힘 직전 짝 없는 `)`·`]`·`}`(전각 포함) 단독 텍스트 노드 제거
- `salvageMalformedMiniMaxSlideMarkup` 체인 배선

### 3. 프롬프트 ☑

- `deck-framework.ts`: 금지 fallback에 `#111c33`·`#0b1220`·`#1e3a8a`·blue-tint 본문(`#60a5fa`/`#93c5fd`/`#cbd5e1`/`#e0e7ff`)·`linear-gradient(#2563eb,#7c3aed)` 추가. "배경을 칠하지 않은 섹션에 밝은 글자 금지 · 밝은 paper 위 유리 카드 금지" 규칙 신설
- `system.ts` 대비 문단에 같은 취지 보강

## 오진 정정 — flow 래퍼 패딩은 이중 적용이 아니다

상위설계 2-2는 **틀렸다.** `position:absolute; inset:0` 자식의 컨테이닝 블록은 host의 **padding box**(= 패딩을 포함하는 영역)라서 host 패딩은 절대 자식을 들여쓰지 않는다. Chromium 실측:

```
host 400×300 (padding:100px) → flow(inset:0) = 0,0,400,300 → 본문 x=100
```

즉 래퍼에 복사된 패딩이 **유일한** 여백이고, 이를 제거하면 본문이 1920×1080 가장자리에 붙는 회귀가 된다. 관련 probe 테스트 11종(`chat-leak-probe-round57/176/179/391/392/417/421/446/478/503` 등)이 이 동작을 이미 고정하고 있었다.

- 실측 전 잠깐 들어갔던 패딩 제거 변경(`wrapFlowOpenTag` / `slimExistingFlowOverlay`)은 되돌렸고, 함께 수정됐던 기대값도 원복했다.
- 대신 `tests/deck-fixed-canvas.test.ts`에 컨테이닝 블록 사실을 근거와 함께 고정하는 케이스를 추가했다.
- 스크린샷의 넓은 상하 여백은 패딩이 아니라 **콘텐츠 밀도** 문제다(카드 3장 × 2줄이 세로 중앙 정렬).

## 검증

- `tests/heal-official-look-contrast.test.ts` 9/9 ☑
- `tests/deck-fixed-canvas.test.ts` ☑ · `tests/template-clone-fill.test.ts` ☑ · probe 라운드 전체 ☑
- `packages/contracts` 타입체크 clean ☑
- 전체 스위트 잔여 실패 7건(`kami-deck` leftover · 카탈로그 demo noun 3종 · Daisy 킷 · cardish imbalance · salvage 픽스처)은 **베이스라인 `8288a0d69c`에서도 동일하게 실패** — 이번 변경과 무관 ☐(별도 처리 필요)

## 협업 사고 기록

다른 세션(soyeon812)이 같은 워크트리에서 동시에 작업해, 검증 전이던 제 작업 트리가 커밋 `a8b7f5a767`로 통째로 커밋됐다. 그 커밋에는 위의 잘못된 패딩 변경과 그에 맞춰 고쳐진 기대값이 포함돼 probe 테스트 11종이 red 상태였다. 후속 커밋에서 패딩 부분만 되돌려 green으로 복구했다.

## 남은 일

- heading 숫자어(「4가지 핵심 기능」) vs 렌더 항목 수 불일치 → top-up 또는 heading 정정 (가드 부재)
- 요금제/카드 본문이 한 단어(`SSO`)로 끝나는 저밀도 카드 top-up
- 세로 중앙 정렬 + 저밀도로 생기는 빈 밴드 — fill-track 일반화 검토
- 3분 1회 미완성: [0908-N02](./0908-N02-1-상위설계-[스톨_부분덱_salvage_이어쓰기].md) 스톨/이어쓰기 후속

## 변경 이력

| 2026-09-08 16:50 | 루프478 구현·검증·오진 정정 |
