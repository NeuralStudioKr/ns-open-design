# Teamver Design — EC2·배포·인프라

**인프라 SSOT.** Staging / Production **EC2 분리**, AWS RDS 전용 DB, 도메인, TLS, Terraform·운영 runbook.

**관련:** [05 OD UI 재사용](./05_OD_UI_재사용_빠른출시.md) · [06 Docs/Slides형 연동](./06_Docs슬라이드형_연동.md) · [04 구현 우선순위](./04_구현_우선순위.md)

**Terraform:** `ns-teamver-devops/terraform/services/teamver-design/` — [`RUN.md`](../../../ns-teamver-devops/terraform/RUN.md) 4절

**운영 runbook (코드):** `deploy/teamver/`

| 환경 | runbook |
|------|---------|
| Staging | `deploy/teamver/devops/nginx/README.md` |
| Production | `deploy/teamver/docs/DEPLOY-AWS.md` |
| 연동 | `deploy/teamver/docs/TEAMVER_APPS_INTEGRATION.md` |
| SDK vendor | [08 Teamver SDK vendor·배포](./08_Teamver_SDK_vendor와_배포.md) |

---

## 한 줄 결론

> **Staging EC2와 Production EC2를 분리**하고, slides/docs 와 같이 **UI·API 서브도메인 2개**로 노출한다.  
> 인프라는 **AWS EC2 + shared RDS** (`teamver_design*`), Production TLS는 **ALB(ACM)**, Staging TLS는 **EC2 Nginx + certbot**.

---

## 1. 왜 호스트를 나누나

| 결정 | 내용 |
|------|------|
| **stg ≠ prod EC2** | 데이터·토큰·장애 격리. slides(`stg-slides*`)·docs(`docs*`) 와 동일 원칙 |
| **1 EC2에 stg+prod 공존 안 함** | 리소스 경합·설정 실수·OD_DATA_DIR 혼선 위험 |
| **Main BE는 또 다른 호스트** | `stg-api` / `api.teamver.com` — Design EC2 와 분리 (기존 Teamver 인프라) |
| **DB는 compose Postgres 아님** | **AWS RDS** (global shared) — 환경별 database `teamver_design_staging` / `teamver_design` |

### 다른 AI Apps 와의 대응

| 앱 | Staging | Production | 패턴 |
|----|---------|------------|------|
| Slides | `stg-slides` / `stg-slides-api` | `slides` / `slides-api` | AI Apps Staging VM · prod 별도 |
| Docs | (통합 prod VM) | `docs` / `docs-api` | FE+BE 서브도메인 |
| **Design** | `stg-design` / `stg-design-api` | `design` / `design-api` | **동형** + OD sidecar |

Design 만 OD daemon(`:7456`)이 UI+API를 한 포트에서 서빙한다. Teamver 측은 여전히 **UI 도메인 + API 도메인** 2개를 둔다.

---

## 2. 전체 토폴로지

```text
                    ┌─────────────────────────────────────┐
                    │  Main BE (기존)                       │
                    │  stg-api.teamver.com / api.teamver.com│
                    └──────────────▲──────────────────────┘
                                   │ bootstrap · session-check
┌──────────────────────────────────┼──────────────────────────────────┐
│ Design Staging EC2 (AWS)         │ Design Production EC2 (AWS)       │
│                                  │                                  │
│ stg-design.teamver.com ──► :7456 │ design.teamver.com ──► :7456     │
│   (OD web + daemon)              │   (OD web + daemon)              │
│ stg-design-api ──────────► :16000│ design-api ──────────► :16000    │
│   (teamver-design-api)           │   (teamver-design-api)           │
│                                  │                                  │
│ TLS: EC2 Nginx + Let's Encrypt   │ TLS: ALB (ACM) → EC2 nginx :80   │
│ DNS: EIP (Terraform)             │ DNS: ALB alias (Terraform)       │
└──────────────────────────────────┴──────────────────────────────────┘
         │                                    │
         └──────────────┬─────────────────────┘
                        ▼
              AWS RDS (shared) — teamver_design_staging / teamver_design
```

**브라우저 흐름**

1. Main FE → AI Apps → Design (Cookie SSO refresh)
2. `stg-design` / `design` — OD UI (nginx 로그인 게이트 + `OD_API_TOKEN` 주입)
3. OD web → `stg-design-api` / `design-api` — SSO·bootstrap·usage

---

## 3. 호스트별 구성

### 3.1 Design Staging EC2

| 항목 | 값 |
|------|-----|
| **AWS 타입 (권장)** | `t3.large` (2 vCPU, **8 GiB**) — OD + design-api + export 여유 |
| **디스크** | root 50GB + EBS data 30GB (`OD_DATA_DIR`) |
| **DNS** | `stg-design.teamver.com`, `stg-design-api.teamver.com` → **EIP** (Terraform) |
| **env** | `.env.staging` — `POSTGRES_HOST` = shared RDS endpoint |
| **기동** | `bash scripts/run_docker.sh --staging --rds` |

**컨테이너** (RDS 사용 시 `design-db` 없음)

| 서비스 | 호스트 포트 | 비고 |
|--------|-------------|------|
| `open-design-daemon` | 7456 | loopback only |
| `teamver-design-api` | 16000 | loopback only |

**Nginx:** `stg-design.teamver.com.{http,https}.conf` — TLS 종료, `auth_request` → `stg-api.teamver.com`

### 3.2 Design Production EC2

| 항목 | 값 |
|------|-----|
| **AWS 타입 (권장)** | `t3.large` (2 vCPU, 8GB) — MVP |
| **스케일 업** | 동시 사용·export 증가 시 `t3.xlarge` |
| **디스크** | root 60GB + EBS data 50GB |
| **DNS** | `design.teamver.com`, `design-api.teamver.com` → **ALB** (EC2 private subnet) |
| **env** | `.env.production` |
| **기동** | `bash scripts/run_docker.sh --production --rds` |

**Nginx:** `design.teamver.com.http.conf` only — **EC2 443/Let's Encrypt 사용 안 함** (ALB가 TLS 종료)

**ALB**

- `idle_timeout`: **3600s** (OD SSE)
- 헬스: `/_nginx/health`
- 대상: EC2 nginx **:80** (Host 기반 라우팅은 nginx)

### 3.3 Main BE (기존 — Design 팀이 새로 띄우지 않음)

| 환경 | 호스트 | Design 에서 쓰는 API |
|------|--------|----------------------|
| Staging | `stg-api.teamver.com` | `GET /api/auth/session-check` (nginx) |
| Production | `api.teamver.com` | 동일 |

### 3.4 RDS (전용 database)

| 환경 | Database | 생성 (1회) |
|------|----------|------------|
| Staging | `teamver_design_staging` | `CREATE DATABASE teamver_design_staging OWNER teamver_admin;` |
| Production | `teamver_design` | `CREATE DATABASE teamver_design OWNER teamver_admin;` |

호스트·포트: `terraform output postgres_host` (global shared RDS).  
`POSTGRES_SSLMODE=require` 권장 (AWS RDS).

---

## 4. 리소스 튜닝

OD EC2는 **LLM 서버가 아니다**. daemon은 오케스트레이션·export·파일 I/O; 모델 호출은 BYOK/외부 API.

| 구간 | Staging | Production |
|------|---------|------------|
| `OD_MEM_LIMIT` | `1536m` | `2g` |
| `NODE_OPTIONS` | `--max-old-space-size=1024` | `--max-old-space-size=1536` |
| export 스파이크 | Chrome subprocess +500MB~1GB 일시 | 동일 |

compose 기본 `512m` 은 **로컬 검증용**. Staging/Production EC2 에서는 위 값 사용.

**디스크:** EBS `OD_DATA_DIR` — **scratch/cache 전용** 목표 ([09 저장소·격리](./09_Design_저장소_격리_출시게이트.md)).  
현재 As-Is는 volume SSOT → **Prod 오픈 전 Phase 0~3 필수**. 용량 모니터링 80% 초과 시 확장 또는 정리.

---

## 5. 환경 변수 분리

| 파일 | 호스트 | 주요 차이 |
|------|-----|-----------|
| `.env.staging.example` | Staging | `POSTGRES_HOST=<RDS endpoint>`, `POSTGRES_DB=teamver_design_staging` |
| `.env.production.example` | Production | `POSTGRES_HOST=<RDS endpoint>`, `POSTGRES_DB=teamver_design` |

**서버에만 두는 비밀 (git 제외)**

- `OD_API_TOKEN` — nginx `teamver-design-od-token.conf` 와 compose `.env` **동일 값**
- `TEAMVER_JWT_SECRET`, `TEAMVER_INTERNAL_API_KEY` — Main BE 와 동기화
- `POSTGRES_PASSWD` — shared RDS master (`TF_VAR_teamver_aws_rds1_pass`)

---

## 6. Nginx · 인증 요약

| 기능 | 구현 | conf |
|------|------|------|
| 로그인 게이트 (OD URL) | `auth_request` → Main BE `/api/auth/session-check` | stg/prod design server |
| OD API 토큰 | `/api/*` 에 `Authorization: Bearer $OD_API_TOKEN` | 동일 |
| SSE | `proxy_buffering off`, 긴 `proxy_read_timeout` | design server |
| design-api | 프록시만 (Cookie SSO는 BE가 처리) | `*-design-api` server |

**금지:** Production 에서 ALB + `design.teamver.com.https.conf` 동시 enable → 리다이렉트 루프.

---

## 7. HA · 확장 (나중)

| 제약 | 내용 |
|------|------|
| OD SQLite | 단일 writer — **EC2 1대** + EBS (출시 전 [09](./09_Design_저장소_격리_출시게이트.md) 로 S3 SSOT 전환) |
| MVP prod | ALB → **OD EC2 1대** |
| 수평 확장 | S3 + Postgres DaemonDb 또는 Track B wrapper BE |

---

## 8. 배포 체크리스트

### Terraform (콘솔 없이)

```text
[ ] global apply (shared RDS, VPC)
[ ] teamver-design staging apply (staging.terraform.tfvars)
[ ] teamver-design prod apply (prod.terraform.tfvars + backend-prod.hcl)
[ ] CREATE DATABASE teamver_design_staging / teamver_design
[ ] teamver.com DNS — Route53 lookup 또는 dns_manual
```

### Staging EC2 (최초)

```text
[ ] SSH (terraform output ssh_command)
[ ] sync deploy/teamver → /opt/teamver-design
[ ] cp .env.staging.example .env.staging (RDS host, secrets)
[ ] bash scripts/run_docker.sh --staging --rds
[ ] teamver-design-od-token.conf 배치
[ ] nginx HTTP → certbot → HTTPS (README.md 3절)
[ ] curl https://stg-design.teamver.com/_nginx/health
[ ] Main FE → Design 카드 동작
```

### Production EC2 (출시)

```text
[ ] cp .env.production.example .env.production
[ ] bash scripts/run_docker.sh --production --rds
[ ] nginx design.teamver.com.http.conf (HTTP only)
[ ] ALB healthy, ACM issued
[ ] curl https://design.teamver.com/_nginx/health
```

---

## 9. 코드·문서 위치

| 항목 | 경로 |
|------|------|
| Terraform | `ns-teamver-devops/terraform/services/teamver-design/` |
| compose | `deploy/teamver/docker-compose.yml` |
| design-api BE | `deploy/teamver/be/` |
| env 예시 | `.env.staging.example`, `.env.production.example` |
| run_docker | `scripts/run_docker.sh` (`--rds`) |
| stg nginx | `devops/nginx/stg-design.teamver.com.*.conf` |
| prod nginx | `devops/nginx/design.teamver.com.http.conf` |
| Main FE URL | `ns-teamver-fe-v2` — `NEXT_PUBLIC_DESIGN_APP_URL` |

---

## 결정 기록

| 일자 | 결정 |
|------|------|
| 2026-06-15 | Staging / Production **호스트 분리** (1 EC2 공존 안 함) |
| 2026-06-15 | slides/docs 동형 — `*-design` + `*-design-api` 서브도메인 |
| 2026-06-15 | **09 저장소·격리** — S3·Litestream·registry Prod gate |
| 2026-06-15 | 인프라 **AWS EC2 + shared RDS** (전용 database) |
| 2026-06-15 | Production TLS = **ALB(ACM)**; Staging TLS = **EC2 certbot** |
| 2026-06-15 | Staging `t3.large` (8 GiB), Production `t3.large` 권장 |

---

## 변경 이력

| 일자 | 내용 |
|------|------|
| 2026-06-15 | 초안 — 호스트 분리·사양·토폴로지·체크리스트 |
| 2026-06-15 | AWS EC2 + RDS + Terraform 반영 |
