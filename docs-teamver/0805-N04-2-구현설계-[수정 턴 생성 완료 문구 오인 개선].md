# 0805-N04-2 구현설계 — 수정 턴 생성 완료 문구 오인 개선

상위: [0805-N04-1](./0805-N04-1-상위설계-[수정%20턴%20생성%20완료%20문구%20오인%20개선].md)

## 변경

### 1. `deckDeliverableProse.ts`
- `looksLikeDeckCreateCompletionProse(text)` — 「생성/초안이 생성/draft is ready/created」 등 생성 완료 주장.
- `shouldHideDeckCreateCompletionProseOnEditTurn({ text, isSlideEditTurn, teamverSlideUi })` — 수정 턴에서 해당 prose 숨김.

### 2. `chat-message-render.ts` / AssistantMessage
- Live lead: `messageLooksLikeSlideEditTurn(message) || messageIndicatesDeckPatchArtifact(...)`.
- Completion lead 분기와 동일하게 `isDeckPatchArtifactTurn` 유지 (이미 preTurn 사용).
- `hasVisibleAssistantTextOutput` / ProseBlock: 수정 턴 + Teamver UI면 create-completion prose를 비가시 처리 → 합성 「수정이 반영되었습니다」 lead가 뜸.

### 3. 테스트
- streaming full `deck` + `preTurnFileNames: ['deck.html']` → edit progress copy.
- completed model prose 「슬라이드 초안이 생성되었습니다.」 on edit turn → edit completion copy, create phrase gone.
- create turn (no preTurn) unchanged.
