# 0807-N03-2 구현설계 — 캔버스→PPT 완료 후 「작성 중」 잔존

## 파일

| 파일 | 변경 |
|------|------|
| `apps/web/src/teamver/deckDeliverableProse.ts` | bare status 인식; `looksLikeDeckInFlightStatusResidue` / `stripDeckInFlightStatusResidue`; settled hide helper |
| `apps/web/src/components/AssistantMessage.tsx` | settled 시 residue 줄 strip + completed lead 재개 |
| `apps/web/src/runtime/chat-message-render.ts` | `hasEmbedVisibleProseBody` 가 strip 후 가시성 판정 |
| tests | deckDeliverableProse · AssistantMessage.teamver-streaming |

## 데이터 흐름

```
stream end (succeeded) + content ≈ "작성 중"
  → stripDeckInFlightStatusResidue → ""
  → hasVisibleAssistantTextBlocks = false
  → shouldSynthesizeTeamverCompletedArtifactLead = true (producedFiles)
  → UI: 「슬라이드 초안이 생성되었습니다」 + 파일 목록

stream end + "작성 중\n\n표지 다음에…"
  → strip → "표지 다음에…" 유지 (completed lead 억제 — 설명 prose 우선)
```

스트림 중에는 status / live lead 그대로 표시.

## 경계

- settled hide 는 **좁은 residue**(bare status · synthetic live-lead · 수정 반영 중)만.
- `looksLikeDeckCreateProgressProse` 장문 진행형은 **숨기지 않음** (Bugbot: 설명 prose 보호).
- edit turn 기존 `shouldHideDeckCreateCompletionProseOnEditTurn` 유지.
