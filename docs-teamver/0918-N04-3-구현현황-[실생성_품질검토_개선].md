# 0918-N04-3 구현현황 · 실생성 품질 검토 후 leftover/구성 갭

상위설계: `docs-teamver/0918-N04-1-상위설계-[실생성_품질검토_개선].md`

## 생성 방법

| 경로 | 결과 |
|---|---|
| 실생성 (staging `https://stg-design.teamver.com`) | **못 함.** 브라우저 MCP가 탭을 열지 못함 (`No browser tab available`). staging HTTP 200은 확인. 로그인·API 키·캡차 우회 없음. |
| 오프라인 LOOK seed | **실행.** official `example.html` + brief `Teamver 소개` → `resolveTemplateCloneSlidesForDeterministicFill` + `buildTemplateClonedDeckHtml` + `salvageMalformedMiniMaxSlideMarkup`. MiniMax 짧은 응답은 seed에서 section 2장만 남긴 뒤 `recoverShortDeckByPaddingToSeed` + salvage. |

fixture: `packages/contracts/tests/fixtures/loop558-live-*.html` (시크릿 없음).

persist / pad / continue / head-banner 정책은 변경하지 않음. healer/fill만.

## 품질 표 (미리보기 vs 생성)

미리보기 = official `example.html`. 생성 = fill+heal. pad = 2장 모델 → seed 장수.

| 킷 | 미리보기 장수 | 생성 | pad | leftover (개요/탐색실행확장/실무자/Halo/$) | 한글 tracking | 레이아웃 | 문장 다양 | pad 밀도 |
|---|---|---|---|---|---|---|---|---|
| Block Frame | 10 | 10 | 10 (pad 4) | 없음. 커버 필은 `표지` | `고객 경험` 유지 | 차트 셸 유지 | 역할 pack + 일부 공통 아웃라인 제목 | kit pack 재적용 |
| Cobalt Grid | 8 | 8 | 8 (pad 7) | 없음 | 유지 | vbig/qr 유지 | 역할 pack | kit pack |
| Product Launch | 8 | 8 | 8 (pad 5) | Halo/`$` 없음 | 유지 | 가격 카드 유지 | topic 결정 카피 | 기존 정책 |
| Grove | 12 | 12 | 12 (pad 2) | 없음 (고침) | 유지 | chart/stat 라벨 채움, 가짜 % 없음 | 역할 pack | kit-aware fill |
| Studio | 12 | 12 | 12 (pad 9) | 없음 (고침) | 유지 | stat/chart 채움 | 역할 pack | kit-aware fill |

고치기 전 오프라인 실측: Grove/Studio에 `개요`/`핵심 포인트`/`실무자`, Block Frame 커버 `개요`, pad 장수가 20/24로 중복(재현 스크립트 슬라이스 버그). 고친 뒤 leftover 0, 장수=미리보기.

## 고친 점

1. **Grove / Studio 역할별 Teamver pack** (`GROVE_KIT_KEY` / `STUDIO_KIT_KEY`). synth·heal·fill이 Cobalt/BF처럼 커버/챕터/stat/list/quote/compare/chart/닫기 문장을 넣음.
2. **leftover 제목·본문** — `실무자/리더/운영자` synth 차단. Grove/Studio heal이 한국어 leftover·영문 카탈로그 크롬(`The Argument`, `Market · Metrics`)도 채움/strip. BF 커버 OVERVIEW→`표지`.
3. **빈 슬롯** — Grove/Studio `bar-col` 차트 라벨을 pack으로 채움. 가짜 % 없음.

## 검증

```bash
pnpm --filter @open-design/contracts exec vitest run \
  tests/loop558-offline-repro.test.ts \
  tests/template-clone-fill.test.ts \
  tests/template-clone-outline.test.ts
```

374 passed.

## 다음 후보

EightBit / Capsule 등 아직 역할 pack이 약한 킷. Grove pad 일부 장은 영문 카탈로그 크롬이 더 남을 수 있음(장식 라벨).

## 변경 이력

| 2026-09-18 15:05 | 오프라인 LOOK seed 실측 후 Grove/Studio pack + leftover/차트 fill. |
