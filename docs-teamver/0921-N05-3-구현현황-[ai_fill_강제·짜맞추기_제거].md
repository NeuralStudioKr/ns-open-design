# 0921-N05-3 구현현황 · AI fill 강제 · 서버 문장 짜맞추기 제거

## 완료

- ☑ `TEMPLATE_CLONE_FILL_DEFAULT_MODE = 'json'` 유지 확인 (`apps/web/src/teamver/templateCloneContentFill.ts:89`) — AI 는 돌고 있음. 11초 완료는 JSON slot-fill outline 응답이 작기 때문. fillMode 정책은 손대지 않았다.
- ☑ `${topic} · N` salt suffix 5경로 제거 (`packages/contracts/src/template-clone-fill.ts`):
  1. `buildTemplateClonedDeckHtml` pad branch — `label = \`${deckTitle} · ${n}\`` → `genericRoleCopyForIndex(...).heading || deckTitle`
  2. `buildTemplateClonedDeckHtml` empty-brief starter — index 0 은 `deckTitle`, 나머지는 `genericRoleCopyForIndex(...).heading || deckTitle`. 이전 `\`${deckTitle} · ${index + 1}\`` 발행 제거
  3. `buildTemplateClonedDeckHtml` enrich fallback — `{ title: index === 0 ? deckTitle : \`${deckTitle} · ${index + 1}\` }` → `{ title: deckTitle }`
  4. `padDeterministicTemplateCloneSlides` dedupe fallback — `if (used.has(label)) label = \`${cover} · ${out.length + 1}\`` 제거 (pack heading 반복 감수)
  5. `rewriteInstructionParrotingSlideTitles` — `\`${fallbackCover} · ${index + 1}\`` → `genericRoleCopyForIndex(...).heading || fallbackCover`
- ☑ 회귀 pin 테스트 추가 `packages/contracts/tests/loop562-server-sentence-assembly.test.ts` — 4 케이스 (starter salt 금지 · resolve outline salt 금지 · bare-domain outline salt 금지 · fixture archive).
- ☑ 사용자 리포트 minimal repro fixture 저장 `packages/contracts/tests/fixtures/loop562-block-frame-broken.html`.

## 별도 슬라이스로 이관 (본 슬라이스 밖)

아래는 리포트에 포함됐지만 광범위 회귀 (kit 4-axis gate · leftover pack · offline repro fixture) 를 유발하므로 별도로 다룬다.

- `serviceIntroSynthTitleFallback` — `${noun} 쓰는 길`, `${noun} 다음` 접미 조합 제거. 여러 kit 의 dense-body 4-axis gate 를 함께 조정해야 안전.
- `synthesizeTemplateCloneCoverLead` fallback — `${topic}이/가 다루는 문제와 제공 가치`, `${topic} — 핵심 맥락과 다음 단계` 두 문장이 `SERVICE_INTRO_LEFTOVER_BODY_RE` 와 substring 겹침. leftover 정규식과 겹치지 않는 문장으로 교체 필요.
- `templatesForSynthTemplateTopic` service-intro preset — 카드 body 8슬라이드 (`이해해야 할 제품 범위와 약속`, `대상 고객별 메시지`, `${topic} 소개는 기능 나열보다…`) 축소.
- Substring strip → leaf-scoped strip 승격 — `wipeEightBitCapsuleLeftoverPhrases` / `wipeGroveStudioLeftoverPhrases` 재작성. 잘림 회귀 원천 봉쇄.

## 검증

- ☑ `pnpm test tests/loop562-server-sentence-assembly.test.ts` — 4 tests pass.
- ☑ `pnpm test tests/template-clone-fill.test.ts tests/template-clone-outline.test.ts` — 본 슬라이스 delta 로 신규 회귀 없음. 남아있는 5 실패는 baseline pre-existing (staging 상태).
  - `deck-framework-compact` (slide-count contract)
  - `deck-quality-slide-count` (renderSlideCountRequirementInstruction)
  - `loop558-offline-repro`, `loop560-offline-repro` (writes live fixtures — 파일시스템 부수효과, staging 상태)
  - `system-prompt-api-mode` (prompt 길이 상한 30_600)
- ☑ ns-open-design 은 ns_cicd 미등록이므로 push 후 CICD subagent 기동 없음. Design staging 은 별도 파이프라인.

## SHA

- 최초 상위설계 커밋 (pre-rename): `754433067b docs(teamver): 0921-N04 AI fill 강제·서버 짜맞추기 제거 상위설계` (origin merge).
- 본 슬라이스 코드 + rename + fixture + status: 이 커밋.

## 재현

```
브리프: "neuralstudio.kr 회사 사이트야. 분석해서 회사 소개 ppt 만들어줘."
템플릿: Block Frame (zhangzara)
```

이전: 표지 subtitle `Neuralstudio가 `, `Neuralstudio 소개 2`, `대상 고객별 메시지 다음`, `측정해야 할 지표 쓰는 길`.

지금: `${deckTitle} · N` salt 접미 5경로 모두 제거. starter path 도 index 0 만 deckTitle, 나머지는 pack heading. `Neuralstudio · 2` / `Neuralstudio 소개 · 2` 회귀 pin.

## 남은 리스크

- **substring truncation** (`사용자가과 전환 비용을 먼저 정의`) 은 본 슬라이스 밖. `SERVICE_INTRO_LEFTOVER_BODY_RE` substring strip 이 여전히 문장 중간을 잘라낸다. leaf-scoped strip 은 광범위 회귀를 유발해 별도 슬라이스에서 다룬다.
- **`${topic} 쓰는 길` / `다음`** 접미도 `serviceIntroSynthTitleFallback` 에 남음. 별도 슬라이스.
- **service-intro preset lines** 도 여전히 서버가 문장을 짜맞춘다 (MiniMax 응답이 짧을 때 pad). preset lines 축소는 다수 4-axis gate 를 함께 조정해야 안전.
