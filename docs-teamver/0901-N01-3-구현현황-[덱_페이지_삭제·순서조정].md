# 0901-N01-3 구현현황 — 덱 페이지 삭제·순서 조정

상위: [0901-N01-1](./0901-N01-1-상위설계-[덱_페이지_삭제·순서조정].md) · 설계: [0901-N01-2](./0901-N01-2-구현설계-[덱_페이지_삭제·순서조정].md)

## 진행

| 항목 | 상태 |
|------|------|
| 상위설계·구현설계 | ☑ |
| `deleteDeckSlideAt` / `moveDeckSlideByDelta` + restamp | ☑ |
| FileViewer deck-nav UI + revision persist | ☑ |
| unit tests (`deck-patch-structure` 5/5) | ☑ |
| i18n (types + 전 locale) | ☑ |
| 코드 리뷰 보완 (HTML 장수 기준 · manualEdit freeze 가드) | ☑ |
| staging bake / 브라우저 QA | ☑ bake node1+node2 (`707997d8be`) · 브라우저 QA ☐ |

## 결정

- Filmstrip은 후속. 1차는 삭제 + ±1만.
- 전용 slides API 없음 — revision POST 재사용.
- 구조 변경은 **디스크 `source`의 `<section class="slide">` 장수**를 기준으로 한다.
- `setManualEditFrozenSource`는 manual edit 모드일 때만 호출.

## 리뷰에서 확인·보완한 점

| 이슈 | 조치 |
|------|------|
| `slideState.count`와 HTML 장수 불일치 | `extractTopLevelSlideSections(html).length`로 clamp |
| manual edit 밖 frozen source | `manualEditMode` 가드 |
| `deckStructureError` 미사용 | deck-nav `title`에 연결 |
| 코멘트 `slideIndex` stale | 알려진 한계 — 후속 remap |
| `div.slide` 호스트 | 이번 범위 밖 (`section.slide` SSOT) |

## 검증

- [x] unit: delete / move / last-slide guard / restamp / edge move
- [ ] 브라우저: 잘못된 장 삭제·순서 이동 후 preview·undo
