# 0806-N05-1 상위설계 — 루트 진입 API 호출 베이스라인

**목적:** Teamver Design home(`/`) cold 진입 Network를 역할별로 고정하고, 호출 횟수 개선의 비교 SSOT로 쓴다.  
**실측:** 2026-08-06 staging embed · 후속: [N06](./0806-N06-1-상위설계-[home%20preview-url%20batch].md) · [N07](./0806-N07-1-상위설계-[home%20cover-html%20batch].md) · [N08](./0806-N08-1-상위설계-[cover%20batch%20harden].md)

---

## 1. 호출 그룹 (역할)

| 그룹 | Path (요약) | 비고 |
|------|-------------|------|
| Boot | `version` · `health` · `analytics/config` · `auth/session` · `session-probe` · `runtime-config` · `app-config` | 필수 |
| 카탈로그 | `skills?catalog=slide` · `design-templates` · `design-systems` · `plugins?mode=deck` · ~~`plugins/example-simple-deck`~~ ([N09](./0806-N09-1-상위설계-[home%20chip%20plugin%20prefetch%20defer].md) defer) | EntryView |
| 목록·배치 | `teamver-bff/projects` · `status-hints` · `cover-hints` · `batch/outputs/latest` | registry |
| 커버 (before) | `/files`×F · `/raw`×N · `preview-url`×N | N=가시 HTML 카드 |
| 커버 (after N06–N08) | `preview-url-batch`×1 · `cover-html-batch`×1 · `/files`×F(miss) · `/raw`×0(hit) | Recent warm gate |

---

## 2. Before 실측 (2026-08-06, N≈6)

```text
# boot + catalog + list (~16)
/api/version … /api/plugins?mode=deck&limit=24
/api/plugins/example-simple-deck
/api/projects/{id}/files          # ×1 miss
/api/projects/{id}/raw/deck.html  # ×6
/api/projects/{id}/preview-url    # ×6
```

합계 대략 **~29** Fetch/XHR (정적·beacon 제외).

---

## 3. After 기대 (N06–N08)

```text
… (boot/catalog/list 동일)
POST /api/projects/preview-url-batch
POST /api/projects/cover-html-batch
# /raw · per-card preview-url 없음 (batch hit)
# /files 는 hints miss만
```

---

## 4. 재실측

Disable cache → home hard reload → Fetch/XHR 복사 → 이 문서 §2/§3에 날짜·N append.

---

## 5. 남은 후보

| 주제 | 기대 |
|------|------|
| ~~chip plugin boot prefetch~~ | **[N09](./0806-N09-1-상위설계-[home%20chip%20plugin%20prefetch%20defer].md)** |
| 카탈로그 탭별 defer | boot 분산 |
| `/files` miss율 추가 축소 | hints 품질 |

---

## 6. 관련

[00 누적 — 0805 home 부트](./00_구현_내역_누적.md) · [0805-N06 `/files` 회귀](./0805-N06-1-상위설계-[프로젝트%20썸네일%20미표시%20회귀].md)
