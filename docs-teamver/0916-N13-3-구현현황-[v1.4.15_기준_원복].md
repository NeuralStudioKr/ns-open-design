# 0916-N13-3 구현현황 · v1.4.15 기준 회귀 원복 (루프546)

## 원인 재확정

`git show v1.4.15:apps/web/src/components/ProjectView.tsx` 로
`findClientSlideCountRegression`를 확인 → v1.4.15 시점 로직과 HEAD 로직이
**동일**(루프545에서 low-substance bypass만 추가 = 더 permissive). 즉
가드 자체는 회귀 원인이 아님.

진짜 회귀는 v1.4.15 이후 도입된 프롬프트 지시(`SLIDE_DECK_UNIQUE_SLOT_COPY_INSTRUCTION`
의 penalty framing — `failed deliverable`, `A deck where a majority of body
slides share the same body sentence is a failed deliverable`)가 모델을
**슬라이드 드롭**으로 유도해 client `slide-count` 가드에 걸린 것.

## 완료

### R1 — `SLIDE_DECK_UNIQUE_SLOT_COPY_INSTRUCTION` 소프트닝

`packages/contracts/src/prompts/deck-quality.ts`

Before:
> Unique-per-slot copy is REQUIRED: … If two slides would end up with identical
> body strings, rewrite one with a distinct angle. Bare one-word labels … are
> forbidden … A deck where a majority of body slides share the same body
> sentence is a failed deliverable. Keep the template's slide count …

After:
> Each card / list item / stat / step / quote body should be a concrete 1–2
> sentence line — prefer a distinct angle per slot over stamping the same lead
> across multiple slots. Do not repeat the slide title as its body. Bare
> one-word labels (핵심, 개념, 요약, 특징, 목표, 방향) as body copy are too
> thin — expand to a real sentence about THIS slot's angle.

- penalty framing(`failed deliverable`, `majority of body slides share the same
  body sentence`) 삭제.
- 밀도 지시(구체 문장 · 제목=본문 금지 · 한 단어 라벨 완화)는 유지.

### R2 — `SLIDE_DECK_KEEP_SLIDE_COUNT_INSTRUCTION` 신규 분리

- `packages/contracts/src/prompts/deck-quality.ts`에 신규 export.
- "장 수 유지" 지시가 유일성 지시와 한 상수 안에 공존해 모델 순응이 흔들리던
  문제를 해소 (사용자 원칙: "슬라이드 수 유지"는 유지 · 동시에 유일성 강요는
  완화).

```
SLIDE_DECK_KEEP_SLIDE_COUNT_INSTRUCTION =
"Deliver the same number of `<section class=\"slide\">` slides as the seed.
 If two slots would repeat, rewrite one with a different angle — do not merge
 or drop slides."
```

### R3 — host contract 두 곳에 신규 상수 삽입

`apps/web/src/teamver/templateCloneContentFill.ts`
- `templateCloneContentFillHardRules()` (JSON slot-fill 턴): topic-lock +
  unique-per-slot + **keep-slide-count** 세 라인으로 emit.
- `buildTemplateClonePromptFillSeed` (prompt-fill 턴): 위 세 상수를 host
  contract에 emit.

### R4 — pin 테스트 갱신

`apps/web/tests/teamver/templateCloneContentFill.test.ts`

- 루프544/546 · prompt-fill seed에 topic-lock + unique-per-slot이 있고 penalty
  framing(`failed deliverable`, `majority of body slides share the same body
  sentence`)은 **제거**되었다.
- 루프544/546 · JSON slot-fill hard rules에도 동일.
- 루프546 · `Deliver the same number of ` 시작 문장이 prompt seed / hard rules
  양쪽에 emit.

### 유지 (원복하지 않음 · 필수 fix)

- `findClientSlideCountRegression` low-substance bypass (루프545) — 더 permissive
  방향.
- rotation salt healer `stripSynthRotationSaltLeaks` (루프545) — 이미 저장된
  salt 잔재 방어용.
- synth outline topic-aware (루프543) — 카탈로그 데모 잔재 제거.
- kit-specific topic-aware fallback (루프543 R2).
- 8-Bit Orbit tier/timeline/stat 슬롯 채우기 (루프540).
- Block Frame invented hero shells / platform card 정리 (루프539).
- cross-kit leftover chrome scrub (루프539).
- Grove leftover catalog copy (루프538).
- block-frame card readability (278d30c5ff).

## 검증

- `pnpm --filter @open-design/contracts build && test` → 3200 tests / 996 files
  green.
- `pnpm exec vitest run tests/teamver/templateCloneContentFill.test.ts` → 44
  pass.
- `pnpm exec vitest run tests/project-view-substance-rich-replacement.test.ts` →
  14 pass (내 pin 통과) · 3 pre-existing 실패는 `/tmp/user-fixture-artifact-regression.html`
  미존재(이번 슬라이스와 무관).

## 최종 표 · v1.4.15 대비 상태

| 항목 | v1.4.15 (`32effa2f9b`) | HEAD (원복 후) | 원복 여부·이유 |
|---|---|---|---|
| `findClientSlideCountRegression` | 로직 A (substance-rich bypass만) | 로직 A + low-substance bypass 추가 | ❌ 원복 X · v1.4.15보다 permissive라 사용자 원칙과 일치 |
| `SLIDE_DECK_UNIQUE_SLOT_COPY_INSTRUCTION` | ✋ 없음 | 소프트닝 (penalty framing 제거) | ⚠️ 부분 원복 · 밀도 지시는 유지 · 회귀 원인이던 penalty 표현 제거 |
| `SLIDE_DECK_TOPIC_LOCK_INSTRUCTION` | ✋ 없음 | 유지 | ❌ 원복 X · penalty framing 아님 · 주제/카탈로그 스크럽 유용 |
| `SLIDE_DECK_KEEP_SLIDE_COUNT_INSTRUCTION` | ✋ 없음 | 신규 export | ✅ 신규 · penalty framing 없이 장 수 유지만 명료히 |
| rotation salt 생성 로직 | ✋ 없음 | ✋ 이미 루프545에서 제거 | 정렬됨 |
| `stripSynthRotationSaltLeaks` healer | ✋ 없음 | 유지 (방어용) | ❌ 원복 X · 이미 저장된 salt 잔재 방어 |
| kit-specific topic-aware fallback | ✋ 없음 | 유지 | ❌ 원복 X · 카탈로그 스크럽 유용 |
| 8-Bit Orbit / Block Frame / cross-kit scrub | ✋ 없음 | 유지 | ❌ 원복 X · 필수 UX fix |

## 남은 리스크 · 사용자에게 재현·비교 방법

- 모델이 여전히 6-8장으로 짧게 낼 수 있음. 이번 완화로 penalty framing이
  사라져 슬라이드 드롭 유인은 크게 감소. 그럼에도 짧게 내는 경우:
  - prior가 **low-substance / seed / leftover** → 저장됨 (bypass).
  - prior가 **substance-rich real content** → 원칙 그대로 거절. 재요청 필요.
- 재현: 같은 project id (`319c5997-…`) / conversation (`8641aba9-…`)로 다시
  요청. staging 배포는 `deploy/teamver/deploy.sh --staging` 이후 minimax-api
  재기동. HEAD `git log -1 --oneline`으로 반영 확인.
