# Design — 세션·인증 · OD 패치·브랜딩 보강

**Track A 출시 전 보안·UX 게이트.** nginx 로그인 게이트와 design-api BFF 골격은 갖춰졌으나, **세션 일관성·토큰 만료 복구·design-api 노출·embed 브랜딩 폐쇄**가 미완이다.

**개발 SSOT:** 본 문서 · [04 구현 우선순위](./04_구현_우선순위.md) · **진행 갱신:** [00 구현 내역](./00_구현_내역_누적.md)

**관련:** [05 OD UI 재사용](./05_OD_UI_재사용_빠른출시.md) · [06 Docs/Slides형 연동](./06_Docs슬라이드형_연동.md) · [08 Teamver SDK vendor](./08_Teamver_SDK_vendor와_배포.md) · [09 저장소·격리](./09_Design_저장소_격리_출시게이트.md) · [11 Usage·Drive Publish](./11_Usage·Drive_Publish_보강.md)

---

## 한 줄 결론

> **Prod 오픈 전에 세션 401 정책 통일·refresh 경로·design-api nginx 게이트·FE auth recovery를 완료하고, embed 모드에서 OD 외부 브랜딩·링크를 중앙 게이트로 폐쇄한다.**

---

## 1. As-Is vs To-Be

| # | 영역 | As-Is (현재) | To-Be (목표) | 우선 | 상태 |
|---|------|--------------|--------------|------|------|
| S1 | `/auth/session` | 토큰 invalid → 200 soft-fail | 토큰 존재·invalid → **401** | **P0** | ✅ |
| S2 | `/bootstrap` vs session | 정책 불일치 | 동일 JWT 검증 | **P0** | ✅ |
| S3 | refresh | SDK `/auth/refresh` 라우트 없음 | design-api **proxy** → Main BE | **P0** | ✅ |
| S4 | design-api nginx | auth_request 없음 | usage/bootstrap 보호 | **P0** | ✅ |
| S5 | FE auth recovery | skipAuthRecovery 차단 | recovery 활성화 | **P0** | ✅ |
| S6 | 로그인 UX | 새 탭 | same-tab redirect | **P1** | ✅ |
| S7 | daemon identity | Main BE identity 헤더 ✅ · nginx auth_request_set ✅ | Phase 3 헤더 주입 | **P1** | 🟡 E2E ☐ |
| S8 | CSRF | Origin 검증 없음 | OriginGuard 미들웨어 | **P1** | ✅ |
| S9 | localhost dev | cookie domain 불가 | Next.js dev rewrite same-origin | **P1** | ✅ |
| S10 | `TEAMVER_JWT_SECRET` | 미사용 | `auth_context.py` JWT 검증 | **P2** | ✅ 유지 |
| P1 | embed topbar | EntryShell 1곳 분기 | `EntryTopbarChips` 추출 | **P1** | ✅ |
| P2 | 브랜딩 | title/favicon/Settings/Help 미패치 | `TeamverBrandingProvider` | **P1** | 🟡 |
| P3 | 브랜딩 env | hostname만 | `VITE_TEAMVER_BRAND_*` | **P1** | ✅ |
| P4 | upstream drift | EntryShell 직접 편집 | `patches/teamver/` | **P2** | ☐ |
| P5 | vendor 정책 | `.gitignore` 상충 | git commit 정책 | **P1** | ✅ |

**범례:** ✅ 완료 · 🟡 부분 · ☐ 미착수

---

## 2. 현재 아키텍처 (As-Is)

```text
Browser (design.teamver.com)
  │
  ├─ GET /              → nginx auth_request → Main BE session-check (204/401)
  │                       → OD SPA (apps/web)
  │
  ├─ GET /api/*         → nginx → daemon (Bearer OD_API_TOKEN, user JWT 없음)
  │
  └─ GET design-api.teamver.com/api/v1/*
        → nginx auth_request (usage·bootstrap)
        → teamver-design-api
              → Main BE bootstrap (internal)
```

**근거 코드**

| 파일 | 라인 | 역할 |
|------|------|------|
| `deploy/teamver/be/app/routers/auth.py` | 33–67 | session soft-fail |
| `deploy/teamver/be/app/routers/bootstrap.py` | 22–48 | bootstrap hard-fail |
| `deploy/teamver/be/app/config.py` | 25–27 | `TEAMVER_JWT_SECRET` dead config |
| `apps/web/src/teamver/designBffClient.ts` | 28–58 | BFF client, skipAuth flags |
| `apps/web/src/teamver/useTeamverSession.ts` | 24–57 | session 폴링, redirect 없음 |
| `apps/web/src/components/TeamverSessionBanner.tsx` | 31–35 | login `target="_blank"` |
| `devops/nginx/stg-design.teamver.com.https.conf` | 62–73, 142–153 | auth_request + design-api open |
| `ns-teamver-be/src/router/auth.py` | 325–362 | session-check + refresh |
| `ns-teamver-be/src/service/auth_cookie.py` | 24–39 | cookie SameSite=Lax, domain `.teamver.com` |

---

## 3. 세션·인증 보강

### 3.1 401 정책 통일 (P0)

**문제:** `/auth/session`은 만료·invalid 토큰에도 HTTP 200을 반환한다. FE는 `authenticated: false`만 보고 login redirect를 하지 않는다.

**수정 — `deploy/teamver/be/app/routers/auth.py`**

```python
# Before (L56–57): bootstrap 실패 → _empty_session() (200)
# After:
async def get_auth_session(request: Request) -> Any:
    token = extract_request_access_token(request)
    if not token:
        return _empty_session()  # 200 — 쿠키 없음 = 비로그인 (정상)

    try:
        bootstrap = await fetch_bootstrap(token)  # optional → required
    except AuthenticationError:
        raise HTTPException(status_code=401, detail="session_expired")

    return { "authenticated": True, "user": ..., "workspaces": ... }
```

**규칙**

| 조건 | HTTP | body |
|------|------|------|
| 토큰 없음 | 200 | `{ authenticated: false }` |
| 토큰 invalid/expired | **401** | `{ detail: "session_expired" }` |
| 토큰 valid | 200 | `{ authenticated: true, user, workspaces }` |

`/bootstrap`과 동일한 `fetch_bootstrap` + `raise_http_for_teamver_error` 패턴을 사용한다.

### 3.2 refresh 경로 (P0)

**문제:** `@teamver/app-sdk` 기본 refresh URL은 BFF base (`design-api.teamver.com/api/v1/auth/refresh`)인데 design-api에 해당 라우트가 없다.

**권장: SDK → Main BE 직결**

`apps/web/src/teamver/designBffClient.ts`에서 `TeamverClient` 생성 시:

```typescript
import { resolveTeamverMainApiBaseUrl } from "./designApiBase";

const client = new TeamverClient({
  baseUrl: resolveDesignApiBaseUrl(),       // BFF
  refreshUrl: `${resolveTeamverMainApiBaseUrl()}/api/auth/refresh`,  // Main BE
  appKey: "design",
  withCredentials: true,
  tokenStore: null,
  onAuthExpired: () => {
    window.location.href = resolveTeamverLoginUrl();  // same-tab
  },
});
```

**`designApiBase.ts`에 추가할 함수**

```typescript
export function resolveTeamverMainApiBaseUrl(): string {
  const host = window.location.hostname.toLowerCase();
  if (host.includes("stg")) return "https://stg-api.teamver.com";
  if (host.endsWith(".teamver.com")) return "https://api.teamver.com";
  return import.meta.env.VITE_TEAMVER_MAIN_API_URL ?? "http://127.0.0.1:8000";
}
```

**대안 (차선):** design-api `POST /api/v1/auth/refresh` → Main BE `/api/auth/refresh` proxy + Set-Cookie relay. cookie domain 이슈로 **Main BE 직결이 더 단순**.

### 3.3 design-api nginx 게이트 (P0)

**문제:** `design-api.teamver.com`은 nginx `auth_request` 밖에 있어 BFF가 공개 노출된다.

**수정 — `deploy/teamver/devops/nginx/stg-design.teamver.com.https.conf` (design-api server block)**

```nginx
# design-api.teamver.com server { ... }

# 공개 허용 (cookie 자체 검증)
location = /api/v1/auth/session { ... proxy_pass design-api; }
location = /healthz { ... }

# 보호 (로그인 필수)
location /api/v1/usage/ {
    auth_request /_teamver_auth;
    error_page 401 403 = @teamver_login;
    proxy_pass http://teamver_design_api;
}
location /api/v1/projects/ {
    auth_request /_teamver_auth;
    error_page 401 403 = @teamver_login;
    proxy_pass http://teamver_design_api;
}
location /api/v1/bootstrap {
    auth_request /_teamver_auth;
    error_page 401 403 = @teamver_login;
    proxy_pass http://teamver_design_api;
}
```

Production `design.teamver.com.http.conf` design-api 블록에도 동일 적용.

**주의:** CORS preflight `OPTIONS`는 auth_request 제외.

### 3.4 nginx auth_request_set — daemon identity (P1, Phase 3 연동)

**2026-06-16 진행:** Main BE `GET /api/auth/session-check`가 검증된 JWT 기준
`X-Teamver-User-Id`, `X-Teamver-Workspace-Id`를 204 응답 헤더로 반환한다. design nginx는
`auth_request_set`으로 해당 헤더를 받아 OD daemon `/api/`와 design-api 보호 라우트에 전달한다.
남은 작업은 staging 배포 후 실제 요청에서 header propagation을 확인하는 것이다.

**design-api 사용 조건:** `TRUST_TEAMVER_PROXY_HEADERS=true`일 때만 design-api가
`X-Teamver-User-Id`, `X-Teamver-Workspace-Id`를 AuthContext로 신뢰한다. 이 값은 nginx
`auth_request` 뒤 내부 upstream에서만 켜고, public 직접 노출 환경에서는 기본값(`false`)을 유지한다.

**목표:** daemon `/api/projects/:id/*` 호출 시 user/workspace 검증에 사용.

**Option A — Main BE session-check 확장 (권장)**

`ns-teamver-be/src/router/auth.py` session-check 응답에 헤더 추가:

```python
@router.get("/session-check", status_code=204)
async def session_check(user: jwtDataDTO = Depends(...)) -> Response:
    return Response(
        status_code=204,
        headers={
            "X-Teamver-User-Id": user.user_id,
            "X-Teamver-Workspace-Id": user.workspace_id or "",
        },
    )
```

nginx:

```nginx
location = /_teamver_auth {
    internal;
    proxy_pass https://teamver_main_be/api/auth/session-check;
    auth_request_set $auth_user_id $upstream_http_x_teamver_user_id;
    auth_request_set $auth_workspace_id $upstream_http_x_teamver_workspace_id;
}

location /api/ {
    auth_request /_teamver_auth;
    proxy_set_header X-Teamver-User-Id $auth_user_id;
    proxy_set_header X-Teamver-Workspace-Id $auth_workspace_id;
    proxy_set_header Authorization "Bearer $teamver_od_api_token";
    ...
}
```

**Option B — design-api access 검증만 (09 P3-9):** daemon middleware → `GET design-api .../access`. nginx 헤더 주입 없이 Phase 3에서 처리.

### 3.5 FE skipAuth 정책 (P0)

**수정 파일**

| 파일 | 변경 |
|------|------|
| `designBffClient.ts` L55–58 | `fetchDesignAuthSession`: `skipAuthRecovery: false` (또는 옵션 제거) |
| `reportUsage.ts` L18–33 | `skipAuthRecovery: false`; 401 시 login redirect |
| `useTeamverSession.ts` L48–49 | `error === "session_expired"` 또는 401 catch → `window.location.href = loginUrl` |
| `TeamverSessionBanner.tsx` L33–34 | `target="_blank"` 제거 → same-tab |

### 3.6 CSRF 방어 (P1)

design-api FastAPI 미들웨어 추가 (`deploy/teamver/be/app/middleware/csrf.py`):

```python
ALLOWED_ORIGINS = {"https://design.teamver.com", "https://stg-design.teamver.com", ...}

async def verify_origin_middleware(request: Request, call_next):
    if request.method in ("POST", "PUT", "PATCH", "DELETE"):
        origin = request.headers.get("origin", "")
        site = request.headers.get("sec-fetch-site", "")
        if site == "cross-site" and origin not in ALLOWED_ORIGINS:
            return JSONResponse(status_code=403, content={"detail": "origin_not_allowed"})
    return await call_next(request)
```

**v1:** Origin + Sec-Fetch-Site 검증. **v2:** CSRF token (double-submit cookie).

SameSite=Lax cookie + CORS `allow_credentials` + origin whitelist로 대부분 CSRF를 차단한다.

### 3.7 localhost dev SSO (P1)

**문제:** Main BE cookie `Domain=.teamver.com` → localhost에서 cookie 전달 불가.

**권장: Vite dev proxy (same-origin BFF)**

`apps/web/vite.config.ts`:

```typescript
server: {
  proxy: {
    "/api/v1": {
      target: "http://127.0.0.1:16000",
      changeOrigin: true,
    },
  },
},
```

`designApiBase.ts` dev 분기:

```typescript
if (import.meta.env.DEV) {
  return "";  // same-origin → vite proxy
}
```

**대안:** `/etc/hosts`에 `local-design.teamver.com` → 127.0.0.1 + stg cookie 공유 (번거로움).

### 3.8 TEAMVER_JWT_SECRET 정리 (P2)

`deploy/teamver/be/app/config.py` L25–27:

- **제거 권장:** design-api는 Main BE bootstrap relay만 사용. 로컬 JWT 검증 불필요.
- **유지 시:** `GET /auth/session` fast-path용 로컬 verify — Docs/Slides와 다른 패턴이므로 **제거가 일관적**.

---

## 4. OD 패치·브랜딩 보강

### 4.1 패치 surface (NEW vs MODIFIED)

| 분류 | 경로 | drift 위험 |
|------|------|------------|
| **NEW** | `apps/web/src/teamver/**` | 없음 |
| **NEW** | `apps/web/src/components/TeamverSessionBanner.tsx` | 없음 |
| **NEW** | `apps/web/src/styles/teamver.css` | 없음 |
| **MODIFIED** | `apps/web/src/components/EntryShell.tsx` | **높음** (3000+ 줄) |
| **MODIFIED** | `apps/web/package.json` (preinstall, app-sdk) | 중 |
| **MODIFIED** | `apps/web/src/index.css` (import 1줄) | 낮음 |
| **MODIFIED** | `pnpm-lock.yaml` | 중 (매 merge) |
| **daemon** | teamver 코드 **0건** | 없음 |

### 4.2 EntryTopbarChips 추출 (P1)

**목표:** EntryShell upstream 충돌 최소화.

**신규:** `apps/web/src/teamver/components/EntryTopbarChips.tsx`

```tsx
export function EntryTopbarChips() {
  if (isTeamverEmbedMode()) {
    return <TeamverSessionBanner teamverEmbed />;
  }
  return (
    <>
      <GithubStarBadge />
      <a className="entry-discord-badge" href={DISCORD_URL}>...</a>
    </>
  );
}
```

**EntryShell.tsx** — 기존 L732–759 블록을 `<EntryTopbarChips />` 한 줄로 교체.

### 4.3 TeamverBrandingProvider — 중앙 embed 게이트 (P1)

**신규:** `apps/web/src/teamver/TeamverBrandingProvider.tsx`

```tsx
type TeamverBranding = {
  embed: boolean;
  brandTitle: string;
  brandSubtitle: string;
  faviconUrl: string;
  showExternalLinks: boolean;
  externalLinks: { github?: string; discord?: string; releases?: string };
};

export function TeamverBrandingProvider({ children }) {
  const embed = isTeamverEmbedMode();
  const value = useMemo(() => ({
    embed,
    brandTitle: import.meta.env.VITE_TEAMVER_BRAND_TITLE ?? (embed ? "Teamver Design" : "Open Design"),
    showExternalLinks: !embed,
    externalLinks: embed ? {} : { github: GITHUB_URL, discord: DISCORD_URL, ... },
  }), [embed]);
  return <TeamverBrandingContext.Provider value={value}>{children}</TeamverBrandingContext.Provider>;
}
```

**적용 대상 (embed 시 숨김/대체)**

| 파일 | 현재 | 수정 |
|------|------|------|
| `app/layout.tsx` L9 | title `Open Design` | `useTeamverBranding().brandTitle` |
| `EntryNavRail.tsx` L62, L106 | `t('app.brand')` | branding context title + favicon |
| `EntrySettingsMenu.tsx` L31, L349 | Discord 링크 | `showExternalLinks && ...` |
| `EntryHelpMenu.tsx` L25, L31 | GitHub/Discord | 동일 |
| `SettingsDialog.tsx` L1343 | releases 링크 | 동일 |
| `AssistantMessage.tsx` L78 | Discord 피드백 | embed 시 숨김 |
| `DesignFilesPanel.tsx` L174 | Discord 팁 | embed 시 숨김 |
| `HandoffButton.tsx` L27 | open-design.ai/amr | embed 시 숨김 |
| `UpdaterPopup.tsx` | 업데이트 UI | embed 시 숨김 (선택) |

**EntryShell wiring:** `EntryShell` root를 `<TeamverBrandingProvider>`로 감싼다 (또는 `EntryShell.tsx` 상위 layout).

### 4.4 브랜딩 env (P1)

**빌드타임 env** (`deploy/teamver/docker-compose.yml` build args 또는 `.env.staging`):

```bash
VITE_TEAMVER_EMBED=1
VITE_TEAMVER_BRAND_TITLE=Teamver Design
VITE_TEAMVER_BRAND_SUBTITLE=AI Design Studio
VITE_TEAMVER_FAVICON_URL=/teamver-favicon.png
VITE_TEAMVER_DESIGN_API_URL=https://design-api.teamver.com
```

**i18n override:** `apps/web/src/teamver/locales/ko.ts`

```typescript
export const teamverKoOverrides = {
  app: { brand: "Teamver Design", brandPill: "Design", brandSubtitle: "by Teamver" },
};
```

i18next init 시 embed 모드면 merge.

### 4.5 patches/teamver/ 도입 (P2)

upstream 수정 파일을 git format-patch series로 관리:

```text
patches/teamver/
  README.md              # 적용/재생성 절차
  0001-entry-shell-topbar-chips.patch
  0002-package-json-app-sdk.patch
  0003-index-css-teamver-import.patch
```

**적용:** `git am patches/teamver/*.patch` (upstream rebase 후)

**재생성:** upstream 파일 수정 후 `git format-patch upstream/main -- apps/web/src/components/EntryShell.tsx`

### 4.6 vendor 정책 정합 (P1)

**현재 상충:**

- `.gitignore` L2–5: `vendor/teamver/app-sdk.tgz`, `python/`, `manifest.json` ignore
- [08](./08_Teamver_SDK_vendor와_배포.md): EC2 단순 배포를 위해 **git commit 권장**

**권장 결정 (택 1):**

| 옵션 | 설명 |
|------|------|
| **A. ignore 해제 (권장)** | `.gitignore`에서 vendor 3파일 제거 → EC2 `git pull`만으로 배포 |
| **B. dist/ 서브트리** | `vendor/teamver/dist/` 에 commit, `.gitignore`는 src ignore 유지 |
| **C. CI artifact** | GitHub Actions artifact → EC2 scp (복잡) |

**Option A 채택 시:** `08` 문서 §6에 "vendor는 git tracked" 명시, `check-teamver-vendor.mjs`는 CI/preinstall에서만 실행.

### 4.7 upstream sync 워크플로우

```bash
# 1. vendor 갱신 (platform SDK 변경 시)
bash scripts/sync-teamver-vendor.sh
git add vendor/teamver/ && git commit -m "chore: sync teamver vendor"

# 2. upstream 동기화
git fetch upstream
git rebase upstream/main
# 충돌 우선 해결:
#   - apps/web/src/components/EntryShell.tsx  (EntryTopbarChips 블록만 유지)
#   - apps/web/package.json                   (@teamver/app-sdk + preinstall 유지)
#   - pnpm-lock.yaml                          → pnpm install 재생성

# 3. 검증
node scripts/check-teamver-vendor.mjs
pnpm install && pnpm --filter @open-design/web typecheck
pnpm --filter @open-design/web build

# 4. embed smoke
#   stg-design.teamver.com 또는 VITE_TEAMVER_EMBED=1
#   - topbar: TeamverSessionBanner, GitHub/Discord 없음
#   - Settings/Help: 외부 링크 없음
#   - title: Teamver Design
```

**브랜치 전략:** `upstream/main` 위에 teamver 커밋을 얇게 쌓고 주 1회 rebase.

---

## 5. 작업 우선순위 · 진행 상황

### Phase S — 세션·인증 (약 1주)

| # | 작업 | 레포 | 상태 |
|---|------|------|------|
| S-1 | `/auth/session` 401 정책 통일 | `deploy/teamver/be` | ✅ |
| S-2 | SDK refresh → design-api proxy | `deploy/teamver/be` | ✅ |
| S-3 | design-api nginx auth_request | `deploy/teamver/devops/nginx` | ✅ |
| S-4 | FE skipAuthRecovery 활성화 + login redirect | `apps/web/src/teamver` | ✅ |
| S-5 | Origin/Sec-Fetch-Site 미들웨어 | `deploy/teamver/be` | ✅ |
| S-6 | Next.js dev rewrite (localhost SSO) | `apps/web` | ✅ |
| S-7 | `TEAMVER_JWT_SECRET` — `auth_context` 사용 | `deploy/teamver/be` | ✅ 유지 |
| S-8 | Staging E2E — 만료·401·refresh | — | ☐ |

### Phase P — OD 패치·브랜딩 (약 1주)

| # | 작업 | 레포 | 상태 |
|---|------|------|------|
| P-1 | `EntryTopbarChips` 추출 | `apps/web/src/teamver` | ✅ |
| P-2 | `TeamverBrandingProvider` + embed 게이트 | `apps/web` | 🟡 |
| P-3 | `VITE_TEAMVER_BRAND_*` env | `apps/web` | ✅ |
| P-4 | i18n override (`teamver/locales/embedOverrides`) | `apps/web` | ✅ |
| P-5 | vendor `.gitignore` 정합 | `ns-open-design` | ✅ |
| P-6 | `patches/teamver/` 초기 series | `ns-open-design` | ☐ |
| P-7 | embed smoke — 외부 링크 0건 | — | ☐ |

---

## 6. 검증 체크리스트 (Staging E2E)

```text
[ ] nginx auth_request — design.teamver.com / (204 → SPA, 401 → login)
[ ] design-api /api/v1/usage/events — auth_request (401 without cookie)
[ ] design-api /api/v1/auth/session — cookie 없이 200 false
[ ] design-api /api/v1/auth/session — expired cookie → 401
[ ] SDK refresh — Main BE /api/auth/refresh → Set-Cookie → session 복구
[ ] FE same-tab login redirect (not _blank)
[ ] embed topbar — TeamverSessionBanner, no GitHub/Discord
[ ] embed Settings/Help — no external OD links
[ ] embed title/favicon — Teamver Design
[ ] localhost dev — vite proxy /api/v1 → design-api
[ ] CSRF — cross-origin POST /usage/events → 403
```

---

## 7. 미결정 · 출시 후

| 항목 | 시점 |
|------|------|
| CSRF double-submit token | Track B |
| nginx auth_request_set + Main BE header | 09 Phase 3 (access 검증과 병행) |
| iframe embed (postMessage SSO) | 필요 시 |
| design-api `/auth/refresh` proxy | Main BE 직결로 대체 시 불필요 |

---

## 8. Track A/B 재정의 (본 문서 범위)

| Track | 본 문서 |
|-------|---------|
| **Track A (출시)** | §3 Phase S + §4 Phase P |
| **Track B (출시 후)** | CSRF token, iframe embed |

자세한 번호: [04](./04_구현_우선순위.md)

---

## 변경 이력

| 일자 | 내용 |
|------|------|
| 2026-06-15 | 초안 — 세션·인증 갭, OD 패치·브랜딩, nginx·FE·vendor 보강 SSOT |
