# 0921-N04-1 상위설계 · AI fill 강제 · 서버 문장 짜맞추기 제거

## 리포트 (루프562)

- 홈에서 `neuralstudio.kr 회사 사이트야. 분석해서 회사 소개 ppt 만들어줘.` 로 생성.
- 11초 만에 종료. 결과물 슬라이드가 서버 synth 문구로 도배.
  - `문제 — 사용자가과 전환 비용을 먼저 정의` ← 헝링 substring strip 잔재
  - `사용자 — 이해해야 할 제품 범위와 약속`, `대상 고객별 메시지 다음`, `측정해야 할 지표 쓰는 길`, `Neuralstudio 소개 2`
  - 모든 텍스트에 `data-od-hangul="1"` — Block Frame Hangul heal 전량 적용
- MiniMax가 낸 문장이 아니라 서버 `templatesForSynthTemplateTopic` service-intro preset + `${noun}` 접미 조합 + salt suffix.

## 원인 (grep · code trace)

| 지점 | 문제 |
|---|---|
| `packages/contracts/src/template-clone-fill.ts` `templatesForSynthTemplateTopic` (service-intro preset, 라인 1782-1863) | 하드코딩 문장 8슬라이드. `이해해야 할 제품 범위와 약속`, `측정해야 할 지표`, `대상 고객별 메시지`, `${topic} 소개는 기능 나열보다…` 등을 서버가 만들어 MiniMax 출력처럼 노출 |
| `serviceIntroSynthTitleFallback` (라인 1610-1616) | `${noun} 범위`, `${noun} 판단`, `${noun} 쓰는 길`, `${noun} 다음` 을 카드 title로 조합 → `Neuralstudio 쓰는 길`, `대상 고객별 메시지 다음` 발생 |
| `padDeterministicTemplateCloneSlides` (라인 2393) 와 `dedupeIdenticalOutlineTitles` (라인 19416) | 중복 title fallback 이 `${cover} · ${n}` 을 발행 → `Neuralstudio 소개 2` |
| `wipeGroveStudioLeftoverPhrases` (라인 15108) · `wipeEightBitCapsuleLeftoverPhrases` (라인 12984) | `.replace(SERVICE_INTRO_LEFTOVER_BODY_RE, '')` 로 문장 중간에서 substring strip → `사용자가 반복해서 겪는 핵심 불편과 전환 …` 이 `사용자가과 전환 …` 으로 잘림 |
| 데몬 `apps/daemon/src/template-clone-deck.ts` LOOK seed | `prompt-fill` 경로에서도 `resolveTemplateCloneSlidesForDeterministicFillWithProvenance` 로 full synth 를 deck.html 에 미리 채움. MiniMax 응답이 슬라이드 수보다 짧으면 seed synth 가 padding 으로 그대로 노출 |

## 원칙 (하지 말 것)

- persist / pad / continue / head-banner **정책 불변**
- 킷 healer 대량 revert 금지 — 문제 hunk 만 최소 되돌림
- `deterministic` 모드 하드코딩 금지 (사용자 opt-in 유지)

## 목표

1. **AI 가 반드시 돈다.** 기본 create/full fill 경로는 MiniMax(JSON slot-fill 유지). 서버는 chrome/layout/leftover 정리만 담당, 슬라이드 문장은 MiniMax 가 만든다.
2. **서버 문장 짜맞추기 제거.**
   - `${noun} · 접미(범위/판단/쓰는 길/다음)` 조합 금지.
   - `${cover} · N` salt / dedupe 접미 금지 (원제 유지).
   - service-intro synth preset — 카드 body 문장 조립 제거. lead 는 짧게, items 는 title-only.
3. **Substring strip 잔재 방지.**
   - `SERVICE_INTRO_LEFTOVER_BODY_RE` 는 요소 전체 텍스트가 정확히 매치될 때만 strip. 문장 중간 substring 제거 금지.
   - 원문에 없으면 아무것도 바꾸지 말 것.
4. **Regression pin.** loop562 라이브 HTML fixture 저장, 잘린 문장·`Neuralstudio · N` salt·`${topic} 다음`/`쓰는 길` title 회귀 pin.

## 변경 범위

| 파일 | 변경 |
|---|---|
| `packages/contracts/src/template-clone-fill.ts` | title fallback / dedupe / pad salt / substring strip / service-intro preset lines 축소 |
| `packages/contracts/tests/loop562-server-sentence-assembly.test.ts` | 회귀 pin (title fallback · dedupe · substring strip · service-intro items title-only) |
| `packages/contracts/tests/fixtures/loop562-block-frame-broken.html` | 사용자 리포트 라이브 fixture 저장 |

## 하지 말 것

- FE `TEMPLATE_CLONE_FILL_DEFAULT_MODE` / `getTemplateCloneFillMode` 상수 변경
- 데몬 endpoint 라우팅 · manifest 필드 변경
- `stripSynthRotationSaltLeaks` / `SERVICE_INTRO_LEFTOVER_BODY_RE` 소스 라인 대량 삭제

## 변경 이력

| 2026-09-21 15:35 | 리포트 · 원인 · 원칙 · 목표 · 변경 범위 확정. |
