# 0914-N10-2 구현설계 — Retry 선택 템플릿 핀 복구

상위: [0914-N10-1](./0914-N10-1-상위설계-[Retry_템플릿_핀_소실].md)

## contracts / 생성

변경 없음.

## web

`apps/web/src/runtime/selected-deck-template.ts`

- `looksLikeExplicitVisualDeckTemplateId` — `example-simple-deck` 제외
- `selectedDeckTemplateMetadataFromRunContext`
- `mergeRetryDeckTemplateIntoSendMeta`
- `findLatestExplicitDeckTemplateFromMessages`

`ProjectView.handleSend`

1. 기존처럼 project metadata로 enrich.
2. `resolveRetryTarget` 이후 원본 user `runContext`를 merge하고 다시 enrich.
3. Retry가 아니고 project/turn 핀이 없으면 대화에서 마지막 explicit 핀을 복구 (컴포저 재입력).

우선순위: 이미 있는 turn meta → retry/history user → project metadata.

## 테스트

- simple-deck을 explicit 핀으로 보지 않음
- 빈 project metadata + retry runContext → Studio 핀 복구 + skillIds
- 기존 turn meta가 retry origin보다 우선
- 대화 history에서 마지막 explicit 핀
- ProjectView 소스 스캔: `mergeRetryDeckTemplateIntoSendMeta` · `findLatestExplicitDeckTemplateFromMessages`
