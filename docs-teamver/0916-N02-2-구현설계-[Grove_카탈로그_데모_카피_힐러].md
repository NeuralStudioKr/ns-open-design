# 0916-N02-2 구현설계 — Grove 카탈로그 데모 카피 힐러

상위: [0916-N02-1](./0916-N02-1-상위설계-[Grove_카탈로그_데모_카피_힐러].md)

## 파일 · 함수

### `packages/contracts/src/template-clone-fill.ts`

1. 정규식
   - `GROVE_STAT_VALUE_DEMO_RE` — `^(?:73\s*%|4\.8\s*[×xX]|#1)$`
   - `GROVE_LEFTOVER_BODY_RE` — landscape / brands that will lead / Strategy · Presentation / Prepared by / Confidential / IMAGE PLACEHOLDER / distrust brand-created / most radical thing a brand can do
   - `GROVE_SIDEBAR_LEFTOVER_RE` — Strategy · Presentation / The Thesis / The Evidence / By The Numbers / Our Approach / Before / After
   - `LEFTOVER_CATALOG_PHRASE_RE` 앞에 Grove 문장만 prefix. `73%`는 넣지 않음.
   - `looksLikeLeftoverTemplateDemoDeck`에도 같은 문장.

2. `fillGroveLeftoverKitSlide(body, attrs, input)`
   - `fillStudioKitSlide` 호출 (공유 cover/chapter/lead/compare/quote).
   - `grove-stat` 시퀀스: label = card title, value = metric or demo→ordinal.
   - leftover `grove-sidebar`만 kicker/title로 교체.
   - leftover heading(`h1`/`h2`/`kicker`/`lead`)만 outline title/lead로 교체.

3. `healGroveLeftoverCatalogCopy(html, brief)`
   - `officialLookIsGrove` 아니면 원본.
   - brief·본문 모두 비어 있고 Hangul도 없으면 원본 (공식 example no-op).
   - leftover(`looksLikeLeftoverTemplateDemoDeck` 또는 `GROVE_LEFTOVER_BODY_RE`) 슬라이드만 fill.
   - 마지막 `stripStudioCreativeCatalogDemoCopy(stripLeftoverCatalogDemoPhrases(out))`.

4. `applyFillHealers` — `healBroadsideLeftoverCatalogCopy` 다음.

### 테스트

- `packages/contracts/tests/fixtures/loop538-grove-teamver-leftover.html`
- `루프538 — Grove forest kit demo chrome … is scrubbed`
- topical Grove(`12` / Hangul, leftover 문장 없음)는 불변.
- 공식 English `example.html` + brief 없음 → no-op.
- 기존 Studio / Broadside leftover 테스트 유지.

## 실행 순서

1. 정규식 · fill helper · healer · wiring.
2. Fixture + 테스트.
3. `pnpm --filter @open-design/contracts test --run template-clone-fill`
4. Studio / Broadside leftover 회귀.
