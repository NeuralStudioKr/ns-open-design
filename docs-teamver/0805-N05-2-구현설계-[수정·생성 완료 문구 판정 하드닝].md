# 0805-N05-2 구현설계 — 수정·생성 완료 문구 판정 하드닝

## `chat-message-render.ts`

```ts
isPrimaryDeckFileName(name) // deck.html / deck-2.html (canonical; not about.html)
messageIndicatesSlideEditArtifact(content) // deck-patch|slide-patch|element-patch
messageLooksLikeSlideEditTurn:
  1. slide edit artifact in body (incl. live type)
  2. preTurn has primary/canonical deck filename
  // NOT: any leftover .html, NOT: produced ∩ non-deck HTML
```

## `deckDeliverableProse.ts`

- create completion: built/finished/ready/완성/준비/here is your deck (+ existing)
- create progress: 작성 중/생성 중/Creating the slide/making your deck
- edit turn hides both progress and completion create claims
- mixed "created and updated" → still hide on edit (prefer edit lead)

## `AssistantMessage.tsx`

- use shared `messageIndicatesSlideEditArtifact` (drop local drift)
- live lead uses hardened `messageLooksLikeSlideEditTurn`

## `ProjectView.tsx` send path

- `resolveCanonicalDeckFileForEdit` — auto-attach / `existingDeckEdit` only when a canonical deck exists
- leftover `about.html` must not trigger existing-deck edit instruction
- existing-deck instruction forbids create-toned status sentences

## prompts `system.ts`

- UNIFIED / COMMENT_EDIT / deliverable override: edit follow-ups → 「수정 반영 중」/「Applying your edits」; prefer patch when comments attached
