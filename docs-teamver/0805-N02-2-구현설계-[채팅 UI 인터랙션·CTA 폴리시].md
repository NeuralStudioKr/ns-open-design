# 0805-N02-2 구현설계 — 채팅 UI 인터랙션·CTA 폴리시

상위: [0805-N02-1](./0805-N02-1-상위설계-[채팅%20UI%20인터랙션·CTA%20폴리시].md)

## 1. Design artifact 단일 클릭

`ChatPane.tsx` `ImportedDesignArtifacts` (또는 동등):

```tsx
onClick={openable ? openFile : undefined}
// keep onKeyDown Enter/Space; remove onDoubleClick (or keep as no-op alias)
```

CSS (`routines.css`):

- hover/focus/active를 `[role='button']`와 `.chat-design-artifact-more`에만.
- `:active` → `var(--bg-muted)` 또는 accent tint.

테스트: `ChatPane.imported-folder-artifacts.test.tsx` — `fireEvent.click`으로 Open 검증.

## 2. Questions banner answered

```css
.questions-banner-answered:disabled {
  opacity: 1;
  cursor: default;
}
```

## 3–6. CTA / jump / composer / error ghost

토큰 패턴 (N01 toast/produced Open과 동일):

```css
:hover → accent or bg-subtle
:active → accent-hover / bg-muted
:focus-visible → outline 2px solid var(--accent); outline-offset 2px
transition → var(--dur-quick) var(--ease-out)
```

적용 대상:

- `.chat-error-action`, `.amr-card__cta` (`chat.css`)
- `.chat-jump-btn` (`composio.css`) + `aria-label={t('chat.scrollToLatest')}`
- `.composer-import`, `.composer-research` (`chat.css`)
- `.chat-error-actions .ghost` (`chat.css`)

## 검증

- Vitest imported-folder-artifacts (click)
- 소스/CSS 회귀 테스트 (선택, 저비용)
