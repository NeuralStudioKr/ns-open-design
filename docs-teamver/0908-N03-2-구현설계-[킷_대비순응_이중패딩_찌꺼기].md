# 0908-N03-2 구현설계 — 킷 대비 순응 · flow 이중 패딩 · 찌꺼기 (루프478)

상위: [0908-N03-1](./0908-N03-1-상위설계-[킷_대비순응_이중패딩_찌꺼기].md) · 현황: [0908-N03-3](./0908-N03-3-구현현황-[킷_대비순응_이중패딩_찌꺼기].md)

## 파일

| 경로 | 역할 |
|------|------|
| `packages/contracts/src/html/heal-official-look-contrast.ts` (신규) | 실효 배경 대비 판정 + 인라인 색 스냅 + 유리 카드 틴트 |
| `packages/contracts/src/html/heal-ai-generated-deck.ts` | 체인 종단에 대비 순응 호출 |
| `packages/contracts/src/html/deck-fixed-canvas.ts` | flow 래퍼 패딩 이중 적용 제거 |
| `packages/contracts/src/template-clone-fill.ts` | 고아 텍스트·스트레이 괄호 스크럽 확장 |
| `packages/contracts/src/prompts/deck-framework.ts` · `prompts/system.ts` | 금지 팔레트 지문 + 대비 규칙 |
| `packages/contracts/src/index.ts` | 신규 모듈 export |

## 1. 대비 순응 (`conformInlinePaletteToOfficialLook`)

hex 덴리스트로는 발명된 팔레트를 못 따라간다. **WCAG 명도비**로 판정한다.

### 1-1. 게이트

- `data-od-official-look-css` 존재
- look CSS `:root`에서 paper 계열(`--paper|--cream|--bg|--background|--surface|--offwhite|--canvas`)과 ink 계열(`--ink|--text|--fg|--foreground`) 토큰을 각각 하나 이상 해석
- 두 토큰 명도비가 충분히 벌어져 있어야 함(≥ 3.0). 아니면 no-op

### 1-2. 배경 추적

슬라이드 섹션마다 태그를 순회하며 유효 배경 명도를 스택으로 관리한다.

- 루트 = 섹션 인라인 `background` 또는 paper
- 인라인 `background`/`background-color`/`background-image` → hex · `rgb()` · `rgba()`(알파는 부모와 합성) · `var(--token)` · gradient(색 정지점 평균)
- **class 배경 불확실:** look CSS가 배경을 칠하는 셀렉터의 class 토큰 집합을 만들어, 그 class를 가진 요소부터는 `unknown`으로 두고 그 하위에서 스냅하지 않는다. `.s-programme .left{background:var(--sun)}` 같은 킷 슬롯을 잘못 뒤집지 않기 위한 안전장치
- `data-od-official-motif-html` 서브트리는 전체 skip

### 1-3. 교정 규칙

| 조건 | 조치 |
|---|---|
| 인라인 `color` vs 유효 배경 명도비 < 3.0 | 배경이 밝으면 ink hex, 어두우면 paper hex로 스냅 |
| 밝은 배경 위 거의 투명한 흰 카드(알파 ≤ 0.35 · 색 명도 높음) | 배경을 `rgba(ink,0.055)` 틴트로 승격 |
| 위 카드의 흰 보더 | `rgba(ink,0.2)` |

이미 판독 가능한 조합(다크 카드 위 `#fbbf24` 등)은 건드리지 않는다.

### 1-4. 배선

`healAiGeneratedDeckMarkup` 종단에서 호출 → persist(6단계) · srcdoc 프리뷰 · preview look heal · PDF export 4경로 모두 커버. 신규 모듈은 다른 heal을 import하지 않아 순환 없음.

## 2. flow 래퍼 패딩 (`deck-fixed-canvas.ts`)

`inset:0` 절대배치는 host의 **패딩 박스** 기준이라 host 패딩이 이미 반영된다.

1. `wrapFlowOpenTag` — host 인라인에 패딩이 있으면 패딩 계열 prop 복사를 건너뛴다.
2. `slimExistingFlowOverlay(inner, hostAttrs)` — 이미 래퍼가 있는(=persist된) 덱에서도 host 패딩이 있으면 래퍼 패딩 선언을 제거한다.

host에 패딩이 없으면 종전과 동일 — 래퍼 패딩을 유지한다.

## 3. 찌꺼기 (`template-clone-fill.ts`)

1. `stripStrayInlineAcronyms` 허용 목록 확장: `R&D`(`R&amp;D`) · `SaaS` · `SSO` · `UX` · `UI` · `ROI` · `B2B` · `B2C` · `PoC` · `CRM` · `ERP` · `SDK` · `SLA` · `QA` · `IoT`
2. `stripStrayBracketTextNodes` 신규 — 블록 태그 사이/닫힘 직전의 짝 없는 `)`·`(`·`]`·`}` 단독 텍스트 노드 제거
3. `salvageMalformedMiniMaxSlideMarkup` 체인의 `stripStrayInlineAcronyms` 옆에 배선 → 4경로 공통

## 4. 프롬프트

- `deck-framework.ts` 금지 fallback 목록에 이번 팔레트 지문 추가: `#111c33` · `#0b1220` · `#1e3a8a` · `#2563eb→#7c3aed` 그라디언트 · `#93c5fd`/`#e0e7ff` 류 blue-200~400 본문
- 신규 규칙 2줄: (a) 배경을 칠하지 않은 섹션에 밝은 글자 금지 — 킷 paper가 비친다, (b) 밝은 paper 위 `rgba(255,255,255,0.0x)` 유리 카드 금지
- `system.ts` 대비 문단(`:1571`)에 같은 취지 한 줄 보강

## 테스트

- `heal-official-look-contrast.test.ts` 신규: 크림 킷 + 다크 SaaS 인라인 → 카드 본문 ink 스냅 · 유리 카드 틴트 · 다크 카드 위 accent 보존 · 킷 class 배경 서브트리 no-op · look CSS 없으면 no-op
- `deck-fixed-canvas.test.ts`: host+래퍼 패딩 중복 제거 · host 패딩 없으면 유지
- `template-clone-fill.test.ts`: `R&D` 고아 텍스트 · 스트레이 `)`
- 프롬프트 스냅샷/문구 테스트 갱신

## 비범위

- heading 숫자어(「4가지」) vs 항목 수 top-up — 후속 슬라이스
- 3분 1회 미완성(스톨·auto-continue 정책) — [0908-N02](./0908-N02-1-상위설계-[스톨_부분덱_salvage_이어쓰기].md) 후속
- 킷 클래스 자체를 쓰도록 강제하는 fill 재설계

## 변경 이력

| 2026-09-08 16:25 | 루프478 구현설계 |
