# 0918-N07-3 구현현황 · Design 크레딧 차감 활성화

상위: `0918-N07-1` · 구현설계: `0918-N07-2`  
브랜치: `feat/design-credit-deduction` (코드는 아직 Registry 골격)

## 2026-09-21 — 경로 전환

- ☑ 상위·구현설계를 **Apps JWT (`TEAMVER_JWT_AUDIENCE=teamver-design`) + M2M consume** 으로 재작성
- ☑ Registry access key · `/api/billing/reserve|commit|balance` 는 설계에서 제외
- ☑ Main에 넣으려던 Registry `GET /billing/balance` 는 커밋하지 않고 되돌림
- ☐ 구현설계 §5 Main spendable / consume
- ☐ Design outbox·가드·토스트를 JWT/M2M 클라이언트로 교체
- ☐ config ON 가드에서 Registry 3종 필수 제거
- ☐ staging E2E (`DISABLED=0`) — Main API 배포 후

이전 체크(킬스위치 통일, B2B/M3 meter, record-only ledger)는 **장부·단가** 범위만 유효하다. Registry HTTP·reserved resume는 더 이상 목표 동작이 아니다.

## 결정

- 앱 식별: `TEAMVER_JWT_AUDIENCE=teamver-design` / `TEAMVER_APP_KEY=design`.
- Main credits HTTP: 둘 다 `TEAMVER_INTERNAL_API_KEY`. 가드에 유저 JWT를 요구하지 않음 (daemon hop).
- 차감 본체: Main `TokenService.effective_spendable_balance` · `consume_after_usage_for_subscriber`.
- 합산 드레인·0 가드·토스트·킬스위치 기본 ON(`DISABLED=1`)은 유지.
- merge ≠ 차감 ON.
- Registry 키를 채우지 않는다. 로컬 `.env.staging`의 `TEAMVER_REGISTRY_*` 는 무시 대상.

## 남은 일

1. ☑ 문서 검토 — 가드는 M2M spendable (daemon에 JWT 없음).
2. Main internal credits API 구현 → Design drain/가드 → 테스트.
3. `DISABLED=0`은 이 경로 E2E 준비된 뒤에만.
