# 0918-N07-3 구현현황 · Design 크레딧 차감 활성화

상위: `0918-N07-1` · 구현설계: `0918-N07-2`  
브랜치: `staging`

## 2026-09-21 — JWT / M2M 구현

- ☑ 상위·구현설계를 Apps JWT (`TEAMVER_JWT_AUDIENCE=teamver-design`) + M2M consume 으로 재작성
- ☑ 가드는 daemon JWT가 아니라 design-api→Main M2M spendable
- ☑ Main `GET/POST /internal/apps/design/credits/{spendable,consume}`
- ☑ Main 전용 테이블 없음. 멱등 = `credit_transaction(design_billing)` + 세션 advisory lock + 부분 UNIQUE
- ☑ Design M2M 클라이언트 · outbox 드레인 · 0 가드 캐시 · estimate policy
- ☑ drain: advisory lock을 잡은 커넥션을 drain 끝까지 유지. `consume_attempted` 후 같은 `reference_id`만 재시도
- ☑ config ON 가드: prices JSON + internal key. Registry 3종 불필요. 코드 기본 `DISABLED=1`
- ☑ daemon policy 매핑. `TEAMVER_BILLING_RESERVE_AMOUNT` 폴백 제거
- ☑ FE 토스트 `TEAMVER_INSUFFICIENT_BALANCE` / `TEAMVER_BALANCE_UNAVAILABLE`
- ☑ env example에서 `TEAMVER_REGISTRY_*` ON 조건 제거
- ☐ staging E2E (`DISABLED=0`) — Main API 배포 후, 명시적 env 변경 시에만

## 결정

- 앱 식별: `TEAMVER_JWT_AUDIENCE=teamver-design` / `TEAMVER_APP_KEY=design`
- credits HTTP: `TEAMVER_INTERNAL_API_KEY`
- 차감: Main `effective_spendable_balance` + `CreditConsumptionPipeline` + `credit_transaction`
- 합산 드레인 300s · 0 가드 · 킬스위치 기본 `DISABLED=1`
- merge ≠ 차감 ON
- Registry 키는 채우지 않는다
- Main에 `app_credit_consume` 테이블을 만들지 않는다

## 남은 일

1. Main·Design push 후 Main credits API를 staging에 올린다
2. `DISABLED=0`은 이 경로 E2E가 준비된 뒤에만
