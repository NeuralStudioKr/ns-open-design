# Home root UI snapshot — 2026-08-13

Pre-wizard rewrite backup for Teamver Home (docs-teamver/61).

Restore: copy files back to `apps/web/src/components/` and `apps/web/src/styles/home/` as needed.
Not imported by the app — archive only.
